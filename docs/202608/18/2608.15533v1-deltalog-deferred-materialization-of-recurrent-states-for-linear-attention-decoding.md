---
title: "DeltaLog: Deferred Materialization of Recurrent States for Linear Attention Decoding"
title_zh: DeltaLog：线性注意力解码中循环状态的延迟物化
authors: "Junqing Lin, Jingwei Sun, Guangzhong Sun"
date: 2026-08-16
pdf: "https://arxiv.org/pdf/2608.15533v1"
tags: ["query:ma-la"]
score: 9.0
evidence: 线性注意力循环状态解码优化
tldr: 线性注意力解码需频繁写回完整循环状态，造成内存流量瓶颈。DeltaLog将状态表示为稠密基础状态加紧凑更新日志，大多数解码步骤仅追加轻量更新，定期合并回基础状态。在多个模型上减少大量状态写流量，更新内核最高加速1.86倍，端到端提升1.05-1.20倍。该方案不改变模型语义，降低推理成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.15533v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 759, \"height\": 367, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 871, \"height\": 437, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 816, \"height\": 538, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1640, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1648, \"height\": 581, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1799, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1799, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 878, \"height\": 378, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 877, \"height\": 371, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 879, \"height\": 372, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 658, \"height\": 494, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15533v1/fig-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 878, \"height\": 445, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.15533v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 443, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15533v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 879, \"height\": 220, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15533v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 843, \"height\": 262, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15533v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 829, \"height\": 411, \"label\": \"Table\"}]"
motivation: 现有解码实现每生成一个token就写回完整循环状态，导致大状态多头模型的内存流量成为主要开销。
method: DeltaLog将循环状态分解为稠密基础状态和有界更新日志，解码时仅追加紧凑更新因子，定期合并回基础状态。
result: 在GDN、KDA、RWKV6上，状态更新内核最高加速1.86倍，写流量减少7.83倍，端到端加速1.05-1.20倍。
conclusion: 提供一种不改变模型语义的循环状态解码方案，有效降低长上下文线性注意力服务的存储与计算开销。
---

## 摘要
线性注意力模型通过将逐对令牌交互替换为循环状态更新，消除了softmax注意力的二次方前缀计算和不断增长的KV缓存。然而，现有的解码实现通常在生成每个令牌后物化并写回完整的循环状态，使得状态维护成为内存流量的一大来源，尤其对于具有大状态和多头数的模型。本文提出了DeltaLog，一种循环状态解码方案，在不改变模型语义的情况下减少这种开销。具体来说，DeltaLog将循环状态表示为密集基础状态加上最近紧凑更新的有界日志。大多数解码步骤仅将紧凑更新因子追加到此日志中，而周期性的合并步骤将累积的更新折回密集基础状态。因此，模型观察到与急切解码相同的密集状态，但大多数全状态写回被轻量级追加操作所取代。我们为GDN、KDA和RWKV6实现了DeltaLog，并将其集成到原型服务栈中。在这些模型上，DeltaLog将循环状态更新内核加速了高达1.86倍，将分析得到的循环状态写流量减少了高达7.83倍，并实现了相对于密集循环基线1.05至1.20倍的端到端服务加速。

## Abstract
Linear attention models eliminate the quadratic prefix computation and context-growing KV cache of softmax attention by replacing pairwise token interactions with recurrent state updates. However, existing decoding implementations often materialize and write back the full recurrent state after every generated token, making state maintenance a major source of memory traffic, especially for models with large states and many heads. This paper presents DeltaLog, a recurrent-state decoding scheme that reduces this overhead without changing the model semantics. Specifically, DeltaLog represents the recurrent state as a dense base state together with a bounded log of recent compact updates. Most decode steps append only compact update factors to this log, while periodic merge steps fold the accumulated updates back into the dense base state. Thus, the model observes the same dense state as in eager decoding, but most full-state write-backs are replaced by lightweight append operations. We implement DeltaLog for GDN, KDA, and RWKV6 and integrate it into a prototype serving stack. Across these models, DeltaLog accelerates the recurrent-state update kernel by up to $1.86\times$, reduces profiled recurrent-state write traffic by up to $7.83\times$, and achieves $1.05$--$1.20\times$ end-to-end serving speedups over dense recurrent baselines.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机与背景）

- **背景**：线性注意力模型通过将 token 间的两两交互替换为循环状态更新，消除了 softmax 注意力的二次方前缀计算和不断增长的 KV 缓存。但现有解码实现通常在**每个生成 token 后都完整物化并写回稠密的循环状态**，导致状态维护成为内存流量的主要来源。
- **核心问题**：论文将这一现象概括为 **“状态更新税”（state-update tax）**——每个 token 仅贡献 \(O(BH(d_k+d_v))\) 个紧凑因子，但急切（eager）物化需要读改写 \(O(BHd_k d_v)\) 个状态元素。随着服务批次（batch）增大，这一稠密状态写回成本成为一阶性能瓶颈，远超机器平衡点，处于强内存受限（memory-bound）区间。
- **整体含义**：该瓶颈源于**物理物化策略**而非循环本身；论文提出 DeltaLog，通过延迟物化改变状态提交的物理调度，在不改变模型语义的前提下降低解码开销，为线性注意力服务提供了一种系统级优化新思路。

## 2. 论文提出的方法论

### 2.1 核心思想
- 将循环状态表示为**稠密基础状态（dense base state）+ 有界紧凑更新日志（bounded log of compact updates）**。
- 大多数解码步骤（append 阶段）仅追加紧凑更新因子，**不触碰稠密基础状态**；每隔 \(M\) 步执行一次**合并（merge）**，将累积更新物化回基础状态。
- 在精确算术下，DeltaLog 与急切稠密解码**代数等价**，模型权重、算子方程、预填充行为完全不变。

### 2.2 关键技术细节
- **追加-合并循环**：设合并间隔 \(M\)，日志容量 \(C=M-1\)。\(C\) 次 append + 1 次 merge 组成一个周期。append 时记录 \((a_t, b_t, \bar{\ell})\)；merge 时执行公式：
  \[
  \bar{S} \leftarrow D(\bar{\ell})\bar{S} + \sum_{i=0}^{L-1} D(\bar{\ell}-\Lambda_{\log}[i]) K_{\log}[i] U_{\log}[i]^\top + a_t b_t^\top
  \]
  随后清零日志与累计对数衰减。
- **状态不变量**（公式 7）：
  \[
  S_t = D(\bar{\ell})\bar{S} + \sum_{i=0}^{L-1} D(\bar{\ell}-\Lambda_{\log}[i]) K_{\log}[i] U_{\log}[i]^\top
  \]
  证明该表示在 append 和 merge 下均对循环递推封闭，保证与急切解码等价。
- **流量模型**：提出公式 (8)–(11)，量化 eager（每次读写完整状态）与 DeltaLog（基础状态读 + 摊销写 + 日志追加/回放）的每步流量差异，说明收益条件。
- **分块内核分解**：沿值维度将头状态划分为多个值分块（value tiles），每个工作单元独立流式处理自己的稠密矩阵分块和值侧日志分块；键侧因子和对数衰减元数据跨分块共享，通过 ReplayPrep（共享预处理）、AppendDecode（并行分块解码）、Finalize（单写者元数据提交）三阶段实现。
- **算子实例化**：覆盖 GDN（头级标量衰减 + Delta 规则）、KDA（按键衰减 + Delta 规则）、RWKV6（按键衰减 + 原始值更新），针对不同衰减粒度和更新语义选取不同合并间隔和精度配置。

## 3. 实验设计

### 3.1 数据集/场景
- **内核级实验**：独立 Triton 解码内核，不涉及具体自然语言数据集；使用合成数据固定形状进行基准测试。
- **服务级实验**：使用 Qwen3.6-35B-A3B（GDN 算子）和 Kimi-Linear-48B-A3B-Instruct（KDA 算子）两个真实模型，每轮生成 128 个解码 token。

### 3.2 Benchmark 与对比方法
- **硬件平台**：H200 NVL（数据中心级）与 RTX 4090（消费级）两块 GPU。
- **对比基线**：
  - **FLA**（Fast Linear Attention）：最强的稠密循环基线，覆盖三个算子；
  - **PyTorch Compile**：torch.compile 优化的稠密 PyTorch 实现；
  - **vLLM**：服务栈中的稠密循环内核路径（仅 GDN 与 KDA 可用）。
- 对比时确保模型、批次大小、解码长度、精度、CUDA graph 配置完全一致。

## 4. 资源与算力

- 论文在硬件配置表（表 2）中给出了 GPU 规格：RTX 4090（24 GB、1008 GB/s、165.2 TFLOPS）和 H200 NVL（141 GB、4.8 TB/s、1671 TFLOPS）。
- 但论文**未明确说明使用的 GPU 数量、总训练或调优时长**、以及具体能耗。所有实验均为推理/解码基准测试，没有涉及模型训练过程。

## 5. 实验数量与充分性

论文设计了以下实验，覆盖面和层次较丰富：

- **内核解码性能**：3 个算子 × 4 个批次（64/128/256/512）× 2 个 GPU 平台，共 24 组对比，报告 TPOT 及相对 FLA 的加速比（图 6、图 7）。
- **机制验证**：H200 上 GDN 在批次 128 下的延迟分解与 DRAM 流量剖析（图 8），直接验证写回减少（写流量降低 7.83 倍）。
- **消融实验**：分块回放（split-replay）与融合回放对比，对 3 个算子在 \(M=4\) 和 \(M=8\)、批次 128–512 下各 6 组实验（图 9）。
- **敏感性分析**：合并间隔 \(M\) 扫描（2–64）和状态大小扩展（128 MiB–1024 MiB）（图 10、图 11）。
- **数值行为**：128 步内核 rollout，3 个算子在批次 64/128/256 下的输出和状态相对误差统计（图 12）。
- **端到端服务**：2 个模型 × 3 个批次，共 6 组配置，报告 TPOT 和加速比（表 4）。

**评价**：实验设计较为客观公平——基线选取了最强的稠密实现（FLA），对比配置严格控制一致；但论文仅验证了 3 个线性注意力算子、2 类模型，未覆盖更多架构；端到端加速仅在批同步图模式下验证，未涉及连续批处理（continuous batching）下的动态调度效果。总体上实验充分性在中上水平，足以支撑论文核心声明。

## 6. 论文的主要结论与发现

- DeltaLog 在 H200 上相对 FLA 加速 GDN 1.34–1.60×、KDA 1.19–1.60×、RWKV6 1.52–1.69×；在 RTX 4090 上分别加速 1.69–1.86×、1.30–1.55×、1.41–1.63×。
- 剖析数据证实，DeltaLog 将 GDN 配置下的每 token 写流量从 0.230 GiB 降至 0.0294 GiB（**7.83 倍减少**），总 DRAM 流量减少 1.64 倍。
- 合并间隔 \(M\) 与状态大小存在最优值；状态越大，DeltaLog 收益越显著（GDN 从 1.48× 增至 1.62×; KDA 从 1.02× 增至 1.58×）。
- 端到端服务中，Qwen/GDN 加速 1.08–1.20×，Kimi/KDA 加速 1.05–1.12×; 收益受 Amdahl 定律约束，小于孤立内核加速。
- 数值误差在可接受范围内（输出最大相对误差 ≤ 0.00450，状态误差更小），满足 BF16 感知的容差标准。

## 7. 优点

- **问题定义清晰**：明确识别了“状态更新税”这一此前未被系统化描述的性能瓶颈，并用 roofline 模型和实测数据加以佐证。
- **方法优雅且可迁移**：基态 + 日志的表示在精确算术下与原始递推完全等价，无需改动模型权重或训练流程，适用范围广。
- **系统级实现扎实**：GPU 内核的分块分解、共享预处理与单写者元数据提交均为实际工程问题提供了可行的解法。
- **实验多维度交叉验证**：从延迟、硬件计数器流量剖析、消融、敏感性和数值行为多个角度验证了机制主张，证据链完整。
- **基线选择严格**：以 FLA（最强稠密 Triton 实现）为主基线，对比公平，结论可信度高。

## 8. 不足与局限

- **实验覆盖有限**：仅覆盖 GDN、KDA、RWKV6 三个算子，未涵盖 RetNet、GLA、DeltaNet 等线性注意力变体；端到端实验也没有覆盖 RWKV6。
- **端到端加速幅度有限**：1.05–1.20× 的实际收益相对孤立内核的 1.19–1.86× 有明显衰减，说明在完整服务栈中，非状态更新部分仍然主导延迟。
- **未明确资源使用**：未报告 GPU 数量、实验耗时、功耗等算力细节，可复现性略有折扣。
- **连续批处理验证不足**：服务原型采用批同步图模式，对连续批处理（continuous batching）下 append/merge 队列切换的开销和调度效果没有充分实验。
- **数据精度风险**：日志因子的低精度存储（BF16）虽然在测试容差内，但在长序列或大状态累积下的极端情况下仍可能产生偏差。
- **结论适用范围受限**：作者也承认，在小批次或小状态场景下收益可能很小甚至为负，方法的适用域主要局限于大规模并发解码场景。

---

（完）
