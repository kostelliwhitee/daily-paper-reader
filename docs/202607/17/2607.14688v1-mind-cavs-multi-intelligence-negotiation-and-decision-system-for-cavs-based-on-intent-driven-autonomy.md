---
title: "MIND-CAVs: Multi-Intelligence Negotiation and Decision System for CAVs based on Intent-Driven Autonomy"
title_zh: MIND-CAVs：基于意图驱动自主的网联自动驾驶车辆多智能体协商与决策系统
authors: "Mainak Mondal, Yihang Feng, Yangchao Luo, Han Song"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14688v1"
tags: ["query:ma-la"]
score: 8.0
evidence: 联网自动驾驶车辆的多智能体谈判与协调
tldr: 当前自动驾驶车辆作为孤立代理运行，仅广播低级运动状态，缺乏高层次意图协调，导致全局决策不一致。本文提出MIND-CAVs框架，通过意图驱动的自主性，让车辆将感知抽象化为结构化意图表示并通过V2X交换，边缘服务器采用学习与规则结合的仲裁机制解决冲突，云端记录决策用于持续训练。在CARLA多车道高速场景中，相比孤立驾驶、先到先得和MARL基线，该方法减少了不安全逼近和非必要刹车，提升了机动完成时间。该工作为网联自动驾驶提供了可解释的意图协商与全局协调机制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14688v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1745, \"height\": 770, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14688v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1828, \"height\": 670, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14688v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 909, \"height\": 362, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14688v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 904, \"height\": 366, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14688v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 906, \"height\": 369, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14688v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1506, \"height\": 585, \"label\": \"Table\"}]"
motivation: 现有CAV缺乏沟通高层次意图的机制，导致全局协调决策不一致，亟需意图驱动的多车协商框架。
method: 车辆抽象感知为意图表示，经V2X交换后由边缘服务器用学习与规则仲裁冲突，云端审计并持续训练。
result: 在CARLA多车道高速场景中，MIND-CAVs相比孤立驾驶、先到先得和MARL基线，机动完成时间更短，不安全逼近和刹车更少。
conclusion: MIND-CAVs通过意图驱动的多智能体协商与边缘协调，实现了全局一致的协同决策，优于现有方案。
---

## 摘要
现代自动驾驶车辆大多作为独立智能体运行：它们依赖车载感知与决策模块，并广播仅暴露低级运动状态的基本安全消息（BSMs）。尽管现有的协同驾驶框架支持有限的传感器共享，但它们很少传达高级机动意图，而边缘计算主要用于内容交付而非决策仲裁。因此，当前的网联自主系统缺乏一种原则性机制来跨车辆做出全局一致、意图感知的协调决策。为解决这一问题，我们提出MIND-CAVs，一种基于意图驱动自主的网联自动驾驶车辆（CAVs）多智能体协商与决策框架。每辆车将原始传感器观测抽象为结构化意图表示，通过V2X链路交换，并从路边边缘服务器接收全局一致的协调计划。边缘智能体结合学习与基于规则的仲裁机制来协商车辆间的冲突意图，而云平台记录决策以供审计和持续再训练。我们在基于CARLA的人工智能在环平台中实现MIND-CAVs，并在涉及冲突机动和路线约束出口的多车道高速公路场景中评估。与独立自主、先到先得仲裁和多智能体强化学习基线相比，实验结果显示机动完成时间改善，不安全接近和多余制动减少。

## Abstract
Modern autonomous vehicles largely operate as isolated agents: they rely on on-board perception and decision modules and broadcast Basic Safety Messages (BSMs) that expose only low-level kinematic state. While existing cooperative driving frameworks enable limited sensor sharing, they rarely communicate high-level maneuver intentions, and edge computing is primarily used for content delivery rather than decision arbitration. As a result, current connected autonomy lacks a principled mechanism for making globally consistent, intent-aware coordination decisions across vehicles. To address this gap, we propose MIND-CAVs, a Multi-Intelligence Negotiation and Decision framework for connected autonomous vehicles (CAVs) based on intent-driven autonomy. Each vehicle abstracts raw sensor observations into structured intent representations, exchanges them over V2X links, and receives globally consistent coordination plans from roadside edge servers. Edge agents combine learned and rule-based arbitration mechanisms to negotiate conflicting intents among vehicles, while a cloud platform records decisions for auditing and continual retraining. We implement MIND-CAVs in a CARLA-based AI-in-the-loop platform and evaluate it in multi-lane highway scenarios involving conflicting maneuvers and route-constrained exits. Experimental results show improved maneuver completion time and reduced unsafe proximity and unnecessary braking compared with isolated autonomy, first-come-first-served arbitration, and multi-agent reinforcement learning baselines.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化、深入且客观的中文总结。

## 论文：MIND-CAVs：基于意图驱动自主的网联自动驾驶车辆多智能体协商与决策系统

### 1. 核心问题与整体含义

*   **研究动机**：当前网联自动驾驶车辆（CAVs）主要作为“孤立智能体”运行。尽管它们通过V2X（车联万物）协议广播基本安全消息（BSMs）来共享低级运动状态（如位置、速度），但无法传达“高级机动意图”（例如，“我要在5秒后变道到最右车道”）。
*   **核心问题**：这种“状态感知”而非“意图感知”的模式导致车辆只能通过短时观测推断周围车辆行为，在密集的交通环境中容易引发保守驾驶、振荡行为、不必要刹车和安全隐患。现有的协同驾驶框架（如协同感知、集中式调度）要么难以与现有V2X栈集成，要么缺乏结构化的协商与仲裁机制，且大多数系统不记录决策原因，缺乏可审计性与透明度。
*   **整体含义**：为弥补这一空白，论文提出MIND-CAVs，旨在构建一个原则性的、全局一致的、意图感知的多车协调决策框架。该框架通过结构化的意图交换和边缘侧仲裁，将协调、实时仲裁和问责制作为可部署V2X架构的核心设计原则。

### 2. 方法论：核心思想与技术细节

*   **核心思想**：采用 **车辆（VI）-边缘（EI）-云（CI）** 的分层架构，将车辆感知抽象为结构化的**意图表示**，通过V2X链路发送至边缘服务器；边缘服务器作为“安全门”，结合学习（VLM）与规则（优先级排班）对冲突意图进行仲裁；云端负责记录决策日志，支持审计与模型持续训练。

*   **关键技术细节**：
    1.  **意图抽象定义**：每个车辆的意图被定义为结构化语义表示 `Iti = ⟨gti, ati, τti, Cti⟩`，包含目标 (`gti`)、机动类别 (`ati`)、执行时间窗口 (`τti`) 和安全约束 (`Cti`)。计划（Plan）`Pti` 则是有限的高层机动原语序列。
    2.  **车辆侧意图生成**：车辆根据局部观测（`oti`，包含位姿、速度、车道索引和压缩地图）和全局路由目标，使用**小型VLM（视觉语言模型）** 生成意图-计划对 (`Iti, Pti`)。
    3.  **边缘侧仲裁机制**：MEC节点维护一份活跃计划数据库 `Dtj`。收到车辆请求后，执行一个**约束型VLM仲裁代理**。该代理接收到结构化的提示（包含当前请求、活动上下文、优先级规则），输出 **ACK（批准）**、**PLAN（修订版计划）** 或 **NACK（拒绝）**。
        *   **冲突检测**：通过确定性时空验证器检查计划是否存在时空冲突（即，两车在相同时间点占据同一车道且距离小于安全阈值 `d_safe`）。若验证失败，则否决为NACK。
        *   **优先级规则**：采用词典排序优先级。紧急车辆优先，否则按请求提交时间先后裁决，平局时按车辆ID裁决。
        *   **计划修订**：如果车辆被优先，MEC将生成一个修订版计划（如延迟变道、减速、插入让行阶段），但只允许一次修订以保证延迟可控。
        *   **有界延迟**：通过单次冲突检测、固定优先级规则、最多一次修订和有界token生成，确保子秒级协商延迟（估算：ACK约335ms，PLAN约690ms）。
    4.  **审计记录生成**：对于每次仲裁，系统生成结构化的审计记录 `Rti = ⟨Iti, Pti, P̂ti, oti, dti⟩`，包含原始意图、计划、获批/修订计划、决策时刻遥测快照和仲裁理由。

### 3. 实验设计

*   **数据集与场景**：使用**CARLA模拟器**（Town 04地图，直四车道高速公路）构建场景，**无背景交通**，所有车辆动力学和传感器配置相同。
    *   **场景S1（单智能体多车道变换）**：两车并排行驶（左1车道和右2车道），左车需横穿4个车道到达最右车道。
    *   **场景S2（冲突车道变换意图）**：初始配置同S1，但左车意图向右至最右车道，右车意图向左至最左车道，形成直接冲突。
    *   **场景S3（受限路线出口机动）**：左车需在60秒内完成多车道变换以驶出右侧出口。
*   **Benchmark**：对比三种基线方法：
    1.  **孤立自主（IA）**：无意图交换，仅依靠车载感知和运动学推理。
    2.  **先到先得集中式仲裁（FCFS）**：一种确定性集中式调度器，按请求到达顺序解决冲突，无意图推理或日志。
    3.  **多智能体强化学习（MARL）**：隐式学习协作策略，但无显式意图协商。

### 4. 资源与算力

*   **算力描述**：论文明确提及使用了**NVIDIA RTX 3070 GPU**（车辆侧VLM推理）、Intel i7-10700 CPU和64GB RAM。MEC侧的仲裁VLM是假设运行在**A100级别硬件**上。
*   **训练时长**：论文**未明确说明**VLM的训练时长、迭代次数或微调策略。仅提到模型是通过结构化的提示来生成意图/仲裁，属于推理阶段评估。文中也未提及任何训练步骤的超参数。

### 5. 实验数量与充分性

*   **实验数量**：**每个场景**都执行了 **10次（10 runs）** 独立随机种子运行（VLM采样引入随机性）。总共进行了 3个场景 × 4种方法 × 10次 = **120次独立实验**。此外，进行了详细的统计分析（均值、标准差、95%置信区间）。
*   **充分性评价**：
    *   **优点**：通过多次重复实验和统计分析，评估了方法的随机稳定性和统计显著性。对比基线涵盖从完全独立（IA）到隐式协作（MARL）的不同范式，对比合理。
    *   **不足**：**实验场景过于简化**。仅涉及2辆车、直道、无背景流量、无交通信号、无行人。方法在多车、密集、城市场景中的泛化性未知。未进行任何消融实验（如，如果移除VLM或安全验证器，性能如何？）。

### 6. 主要结论与发现

*   **效率提升**：在**所有三个场景**中，MIND-CAVs均实现了**最低的机动完成时间**（S1中比IA快约25%），方差也更低，表明协调更稳定。
*   **安全性与稳定性**：MIND-CAVs在S1中实现了**零缺口违规和零不必要刹车**；在S2和S3中，缺口违规和不必要刹车次数也是所有方法中**最低或接近最低**的。相比IA和MARL，不必要刹车显著减少。
*   **核心总结**：通过结构化的意图协商和边缘安全门控，MIND-CAVs能够在无需引入协调延迟的情况下，同时提升多车场景下的**通行效率**和**驾驶安全性**，尤其是在直接冲突和长时机动规划场景中。

### 7. 优点

*   **新颖性**：将“意图”作为显式、结构化、机器可读的协商单元，而不是隐式推断或简单的状态通报。从“感知-决策”转向“意图-协商-决策”的范。
*   **架构设计兼顾实时性与审计性**：设计了车辆-边缘-云三层架构，边缘侧负责低延迟仲裁（通过有界延迟协议），云侧负责离线审计和模型持续训练，分工明确。
*   **可解释性与问责机制**：通过结构化的审计日志，实现了决策链的确定性回放和监管审查，这是现有许多“端到端”黑盒方法所不具备的。
*   **可部署性**：在真实硬件（Jetson AGX, A100）上进行了延迟估算，并采用了主流V2X协议（5G NR-V2X），考虑了实际部署的通信和计算约束。

### 8. 不足与局限

*   **实验场景过于简单**：仅测试了2辆车、直道、无干扰的完美驾驶环境。未测试多车、密集、城市交叉口或存在感知不确定性/噪声的场景。评估的充分性严重不足。
*   **方法局限性**：VLM的使用引入了**随机性**和**推理成本**，且基于VLM的决策不是**形式化验证**的。尽管有安全门控，但VLM可能产生非最优或意外的决策，尤其是在罕见或对抗性场景下。
*   **速度限制**：实验中将所有车辆上限速度固定为50 km/h，这是为了适应外部模型的API速率限制，但**无法反映真实高速公路的变速度行为**，可能对结果产生偏见。
*   **未消融分析**：没有对MIND-CAVs的关键组件（如VLM、安全验证器、优先级规则）进行消融实验，无法量化每个组件的具体贡献。
*   **评估指标**：只使用了时间、缺口数和刹车次数。未评估更宏观的指标，如系统整体吞吐量、平均速度或对单车能耗的影响。也未测试控制器鲁棒性（如，通信延迟抖动时的表现）。

（完）
