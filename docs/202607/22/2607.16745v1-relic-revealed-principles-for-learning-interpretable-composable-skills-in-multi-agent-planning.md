---
title: "RELIC: Revealed Principles for Learning Interpretable Composable Skills in Multi-Agent Planning"
title_zh: RELIC：多智能体规划中学习可解释可组合技能的揭示原则
authors: "Nguyen Viet Tuan Kiet, Bui Dinh Pham, Duong Quoc Chinh, Dao Van Tung, Tran Cong Dao, Huynh Thi Thanh Binh"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16745v1"
tags: ["query:ma-la"]
score: 8.0
evidence: 多智能体规划中私有技能的协调
tldr: 多智能体规划中，各智能体需改进专业技能但保持内部实现私有，现有方法依赖集中优化或共享策略，不适用于隐私场景。RELIC框架通过揭示原则实现可解释可组合技能：每个智能体使用私有的LLM引导搜索优化自身程序化技能，可信协调者仅根据团队表现评估更新。成功的协作行为被抽象为可移植原则，其他智能体可实例化并与本地策略重组。该框架分离了协调与实现共享，在异构技能签名下实现跨智能体迁移，为隐私保护下的技能学习与协调提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16745v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1771, \"height\": 481, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16745v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 855, \"height\": 690, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16745v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1783, \"height\": 1788, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16745v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1806, \"height\": 1656, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16745v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1775, \"height\": 392, \"label\": \"Table\"}]"
motivation: 现有多智能体规划方法假设集中优化或共享策略，无法在智能体保持实现私有的隐私约束下进行协调。
method: RELIC框架：每个智能体通过私有LLM搜索优化程序化技能，协调者仅基于团队表现评估；成功行为抽象为可移植原则供其他智能体实例化。
result: 实现了隐私保护下的跨智能体技能迁移与组合，无需共享代码或执行策略，仅通过抽象原则协调。
conclusion: RELIC提出了一种隐私约束下学习可解释可组合技能的新范式，适用于异构智能体协作规划。
---

## 摘要
当智能体必须提升专门化决策技能同时保持其内部实现私有时，多智能体规划变得更为困难。当智能体独立开发、暴露不同接口和能力，且必须在不共享可执行策略的情况下协调时，就会出现这种情况。先前的研究大多假设集中优化、共享策略访问或通用技能表示，使其不适合隐私约束下的合作。我们提出RELIC，一种通过揭示原则学习可解释和可组合技能的框架。每个智能体通过私有LLM引导的搜索优化其自身的程序化技能，而可信协调者仅通过团队级性能评估提议的更新。成功的行为不以代码形式广播；相反，它们被抽象为可移植的原则，其他智能体可以在自己的接口内实例化这些原则，并与局部策略重新组合。这将协调与实现共享分离开来，从而在异质技能签名下实现跨智能体迁移。因此，RELIC为多智能体规划中保护隐私的技能学习和协调引入了一种新范式。

## Abstract
Multi-agent planning becomes substantially harder when agents must improve specialized decision-making skills while keeping their internal implementations private. This regime arises when agents are developed independently, expose different interfaces and capabilities, and must nevertheless coordinate without sharing executable policies. Prior research has largely assumed centralized optimization, shared policy access, or common skill representations, making it poorly suited to privacy-constrained cooperation. We introduce RELIC, a framework for learning interpretable and composable skills via revealed principles. Each agent refines its own programmatic skill through private LLM-guided search, while a trusted orchestrator evaluates proposed updates solely through team-level performance. Successful behaviors are not broadcast as code; instead, they are abstracted into portable principles that other agents can instantiate within their own interfaces and recombine with local strategies. This separates coordination from implementation sharing, enabling cross-agent transfer under heterogeneous skill signatures. RELIC thus introduces a new paradigm for privacy-preserving skill learning and coordination in multi-agent planning.

---

## 论文详细总结（自动生成）

# RELIC：多智能体规划中学习可解释可组合技能的揭示原则——详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在多智能体规划中，智能体往往由不同团队独立开发，各自拥有私有化决策接口和内部实现，且必须在不共享可执行策略（代码或策略权重）的前提下实现团队协作。现有方法大多假设集中优化、共享策略访问或通用技能表示，无法满足这类**隐私约束下的合作**需求。
- **研究动机**：如何让异构接口的智能体在各自保持实现私有的同时，通过某种抽象机制实现**可解释、可组合的协调行为**，并允许跨智能体迁移和复用成功的协作模式。
- **整体含义**：RELIC 提出了一种新范式——通过“揭示原则”（revealed principles）分离协调与实现共享，使团队能在不暴露内部代码的情况下，从个体成功行为中提取抽象策略并共享给其他智能体，从而在隐私保护下提升团队性能。

## 2. 论文提出的方法论

### 核心思想
- **私有技能搜索**：每个智能体（agent）维护一个私有的程序化技能树（Monte Carlo Tree Search），通过 LLM 引导的编辑操作（Lift、Bridge、Reflect）生成并评估候选程序。搜索完全在本地进行，仅向协调者输出团队级性能评估结果。
- **公开原则树**：协调者（orchestrator）维护一个公共的“原则树”（public tree），存储从成功技能中蒸馏出的文本描述性策略（如“优先靠近队友未覆盖的高价值目标”）。其他智能体可检索并实例化这些原则，适配自身接口。
- **分离机制**：每个智能体最终保留的是自身可执行的程序，而协调结构以文本原则形式共享，既保持隐私又实现可迁移的组合。

### 关键技术细节
1. **私有程序搜索**：
   - 每个智能体 i 维护搜索树 \( T_i \)，节点为候选程序 \( \pi \)，使用 UCT 公式（式 (3)）选择子节点。
   - 在扩展节点上，通过 **Lift（L）**（从本地存档中实例化原则）、**Bridge（B）**（从公共原则树中获取原则）、**Reflect（R）**（基于协调者提供的反思摘要）三种操作符，调用 LLM 生成修订程序 \( \tilde{\pi}_i \)。
   - 评估方式：协调者用新程序替换智能体 i 的当前最佳程序，计算团队性能增益 \( \Delta_b(\tilde{\pi}_i) \)（式 (9)），更新节点和操作符统计量（式 (10)-(12)）。

2. **原则蒸馏与公开树**：
   - 每批次结束后，对每个智能体本轮得分最高的节点（\( v^*_{i,b} \)），通过 LLM 蒸馏映射 \( D_i \) 产生原则文本 \( \rho \) 和类型 \( \tau \)，插入公共原则树。
   - 每个智能体也可将自身当前最优技能抽象为原则 \( \hat{\rho}_{i,b} \)，广播到其他智能体的本地存档 \( A^{rev}_{j,b} \)。
   - 公共原则树具有检索与背书机制：当某个原则被引用并带来正增益时，其累计得分 \( E(p) \) 增加，后续更易被检索（式 (19)）；低分节点定期剪枝。

3. **批量级训练**：
   - 使用混合批 \( \tilde{B}_b = B_b \cup \text{replay samples} \) 避免非平稳流下的短视更新（式 (4)）。
   - 每批次交替执行私有搜索（更新可执行技能）和原则更新（更新公共原则树）。

### 算法流程（文字描述）
1. 初始化各智能体私树（根节点为种子技能），公共原则树为空。
2. 对每个批次 \( b \)：
   - 对每个智能体进行若干轮私人搜索：
     - 使用 UCT 选择节点和操作符，调用 LLM 生成修订版本。
     - 协调者评估团队性能变化，更新树节点统计量。
     - 若比当前最优好，则更新智能体的最优技能。
   - 从每个智能体本轮正增益节点中选取最佳者，蒸馏为原则加入公共树。
   - 广播各智能体最优技能的抽象原则到其他智能体。
   - 公共规则树根据使用情况更新背书分数并剪枝。
3. 返回训练后的团队技能和公共原则树。

## 3. 实验设计

### 数据集/场景
- **六个多智能体规划基准问题**，分为两大类型：
  - **多智能体路径规划**：
    - 奖牌收集问题（Team Orienteering）
    - 区域侦察问题（多机器人信息收集）
    - 动态服务问题（随机到达的时效性服务请求）
  - **多智能体调度**：
    - 作业调度（并行机器加权延迟最小化）
    - 熟练任务调度（技能依赖的异构任务分配）
    - 流水车间调度（三阶段加工，加权完成时间最小化）
- 每个问题设三个智能体（A、B、C），具有不同的函数签名（例如 A 只有本地信息，B 有队友位置，C 有全局向量）。
- 训练集：路径规划用 40 节点实例（每批 10 个，共 5 批），调度问题用 30 或 25 个任务/作业。
- 测试集：在两种更大规模（50 和 80）下各 20 个实例。

### 对比方法
- **非 LLM 基线**：GS（网格搜索）、HHS（超启发式）、CCEA（协同进化）、HAOS（混合自适应算子选择）、RL-IS（基于强化学习的超启发式）
- **LLM 基线**：MOTIF（多策略优化框架，唯一可处理异构函数签名的 LLM 方法）

### 实验设置
- 所有方法均在完全相同的问题接口、种子初始化和搜索预算下比较（MOTIF 与 RELIC 对齐）。
- RELIC 每批次每智能体执行 10 次私有搜索迭代，共 150 步；LLM 使用 gpt-4o-mini，温度 1.0。
- 非 LLM 基线采用集中离线训练模式（全批次或多遍训练），RELIC 则流式在线更新。

## 4. 资源与算力

- 论文**未明确列出使用的 GPU 型号、数量、总训练时长**。
- 仅提及 LLM 后端为 **gpt-4o-mini**（OpenAI 的 API 模型），采样温度 1.0，每批每智能体 10 次调用，共 150 次 LLM 调用/运行。未说明是否使用本地 GPU 加速推理。
- 因此，算力细节不充分，读者无法直接复现资源需求。这可能是因为 LLM 调用主要通过 API，而非本地推理。

## 5. 实验数量与充分性

### 实验数量
- **6 个基准问题** × **2 种测试规模**（50、80）= **12 个主要测试场景**。
- 每个场景对比了 6 种基线方法 + 自己（RELIC），共 7 种方法。
- 提供了训练收敛图（图2）。
- 附录中展示了具体发现的策略代码（4个例子）和基准问题的详细形式化定义。

### 充分性与公平性
- **优点**：覆盖了路径规划和调度两大类任务，智能体接口异构性设计合理；对比方法包括传统搜索、进化、RL 和 LLM 方法，较全面；RELIC 与 MOTIF 在搜索预算上对齐；训练与测试规模分离，避免过拟合。
- **局限**：
  - **消融实验缺失**：未单独分析原则树、私有搜索、操作符（L/B/R）等模块的影响。例如，去掉原则共享或只使用反射的效果未知。
  - **稳定性**：仅基于 5 次运行报告平均值（原文 Table 1 标题注明 5 runs），但未给出方差或置信区间。
  - **基线比较**：非 LLM 基线被描述为“集中离线训练”，而 RELIC 是流式在线训练，本质设定不同，可能不公平；作者指出目的是展示 RELIC 在隐私约束下的优势，但未与允许共享的集中式 LLM 方法（如共享技能库）对比。
  - **规模较小**：训练实例每批仅 10 个，共 50 个，测试最大规模 80，可能不足以代表真实世界的大规模问题。

## 6. 论文的主要结论与发现

- RELIC 在 **12 个测试场景**中有 **10 个**（根据 Table 1）优于所有基线，特别是在“动态服务”和“流水车间”任务上提升显著（如动态服务 80 节点时 RELIC 得分 85.48，远高于 MOTIF 的 63.66）。
- 展示了**隐私保护下的有效跨智能体技能迁移**：原则树允许一个智能体学到的策略（如“优先探索队友远离的方向”）被另一智能体适配并应用，而无需知道原始代码。
- 训练收敛曲线（图2）显示团队中各智能体性能均衡提升，说明原则共享促进了平衡贡献。
- 结论：RELIC 为隐私约束的异构智能体协作提供了可行范式，且可解释、可组合的技能表示优于传统方法。

## 7. 优点

- **隐私保护与可迁移性的巧妙平衡**：通过文本原则而非代码共享，既保持私有实现机密，又实现协调知识的跨智能体复用。
- **可解释性**：最终学习的协调策略以自然语言原则形式存在，人类可以理解、审核甚至手动调整。
- **模块化设计**：私树搜索、公共原则树、三个操作符各司其职，易于扩展（如添加新操作符或原则聚合策略）。
- **适应性**：混合批处理和重放机制应对非平稳任务流，避免灾难性遗忘。
- **实验多样性**：覆盖路径规划与调度两大领域，智能体签名设计精心，体现了异构性挑战。

## 8. 不足与局限

- **消融分析不足**：未验证每个组件（私树、原则树、不同操作符）的单独贡献，难以判断哪些部分最关键。
- **统计可靠性有限**：仅 5 次运行且无方差，结果稳健性存疑；且仅报告了一次特定超参数配置下的结果。
- **计算资源细节缺失**：未说明 LLM 调用成本、训练时间等，不利于成本评估和复现。
- **规模与泛化性**：测试实例规模偏小（最大 80），且仅限于六种特定问题。能否推广到大规模（数百智能体）、真实世界问题（如无人机集群、工厂调度）未知。
- **对比公平性**：非 LLM 基线均为集中离线训练，而 RELIC 为分布式在线训练；与完全集中共享技能的 LLM 方法（如联合搜索所有智能体代码）对比缺失，无法说明隐私约束带来的性能损失。
- **LLM 依赖**：方法严重依赖 LLM 的蒸馏和生成质量，若 LLM 不稳定或产生无效原则，可能影响效果。未讨论失败案例或原则质量度量。
- **扩展性问题**：当前仅 3 个智能体，当智能体数量增多时，公共原则树的检索和背书机制复杂度可能增加，未做分析。

（完）
