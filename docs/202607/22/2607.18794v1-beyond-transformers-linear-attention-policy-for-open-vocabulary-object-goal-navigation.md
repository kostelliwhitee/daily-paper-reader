---
title: "Beyond Transformers: Linear Attention Policy for Open-Vocabulary Object Goal Navigation"
title_zh: 超越Transformer：面向开放词汇物体目标导航的线性注意力策略
authors: "Jiahong Zhang, Yifan Lin, Yandong Zhang, Sijun Shen, Kexin Wang, Yuqi Pan, Hongjuan Pei, Wei Wang, Guoqi Li"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18794v1"
tags: ["query:ma-la"]
score: 9.0
evidence: 为导航智能体提出线性注意力策略
tldr: "开放词汇目标导航（OVON）任务中，基于Transformer的策略网络在上下文窗口上的性能不随长度提升，表明自注意力不适合导航状态整合。为此提出线性注意力导航（LANav），采用线性注意力作为策略主干进行结构化状态更新，并引入加权状态扩展线性注意力（WSLA）进一步细化状态。在HM3D-OVON上平均成功率36.4%，比Transformer基线高6.3个百分点，长距离场景增益更大。实际部署在Unitree Go2机器人上达到82%成功率，验证了高效性与sim-to-real迁移能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18794v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1819, \"height\": 688, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18794v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1824, \"height\": 552, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18794v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 858, \"height\": 315, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18794v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 869, \"height\": 344, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18794v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 878, \"height\": 505, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18794v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 869, \"height\": 631, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18794v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 868, \"height\": 630, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18794v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 747, \"height\": 2064, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18794v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1842, \"height\": 442, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18794v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 884, \"height\": 352, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18794v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 879, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18794v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 884, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18794v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 881, \"height\": 640, \"label\": \"Table\"}]"
motivation: 发现Transformer自注意力在导航状态整合中不随上下文长度扩展，需更有效的结构化状态更新机制。
method: 提出基于线性注意力的LANav，以结构化方式更新状态，并设计WSLA将注意力头状态扩展为子状态并加权读出以增强有效性。
result: "HM3D-OVON上平均SR 36.4%，优于Transformer 6.3%，长距离增益显著，HSSD迁移和微调鲁棒，实机成功率82%。"
conclusion: 展示了线性注意力在OVON中的高效性，提供可部署到真实机器人的方案，强调状态更新设计的关键作用。
---

## 摘要
开放词汇物体目标导航（OVON）要求智能体在部分可观测条件下运行，使得有效的内部状态更新对导航性能至关重要。这种更新由策略网络实现，近期方法采用基于Transformer的主干网络，通过上下文窗口上的自注意力来整合时序信息。然而，我们的控制实验表明，在基于Transformer的策略下，性能并不随上下文长度扩展，这质疑了自注意力在导航状态整合中的适用性。为此，我们提出基于线性注意力的导航（LANav），它采用线性注意力（LA）作为策略主干来维持结构化的状态更新，而非在上下文窗口上使用自注意力。在相同设置下评估的多个LA变体中，LANav始终优于基于Transformer的基线。随着状态更新机制变得更加结构化和规范化，性能得到提升，突显了状态更新设计的重要性。为了提升状态更新效果，我们引入加权状态扩展线性注意力（WSLA），它将每个注意力头的状态扩展为多个子状态，并使用可学习的加权读出机制来聚合扩展后的子状态。配备WSLA后，LANav在HM3D-OVON上实现了36.4%的平均成功率（SR），在宏观平均SR上比基于Transformer的对应方法高出6.3个百分点，同时保持了计算效率。距离分层结果表明在长距离场景中收益更大，而HSSD迁移和微调展示了跨场景分布的鲁棒性。在Unitree Go2上的实际部署进一步在50次试验中实现了82%的成功率，支持了LANav的实践可行性和仿真到现实的迁移。

## Abstract
Open-Vocabulary Object Goal Navigation (OVON) requires agents to operate under partial observability, making effective internal state updates critical for navigation performance. This update is implemented by the policy network, where recent approaches adopt Transformer-based backbones with self-attention over a context window to integrate temporal information. However, our controlled experiments show that performance does not scale with context length under Transformer-based policies, questioning the suitability of self-attention for state integration in navigation. To this end, we propose Linear Attention-based Navigation (LANav), which adopts linear attention (LA) as the policy backbone to maintain a structured state update rather than self-attention over the context window. Across multiple LA variants evaluated under identical settings, LANav consistently outperforms Transformer-based baselines. Performance improves as state update mechanisms become more structured and regulated, highlighting the importance of state update design. To improve state update effectiveness, we introduce Weighted State-Expansion Linear Attention (WSLA), which expands each attention head's state into multiple sub-states and uses learnable weighted readout to aggregate expanded sub-states. Equipped with WSLA, LANav achieves 36.4% average success rate (SR) on HM3D-OVON, outperforming Transformer-based counterparts by 6.3 percentage points in macro-averaged SR, while maintaining computational efficiency. Distance-stratified results show larger gains in long-distance episodes, while HSSD transfer and fine-tuning demonstrate robustness across scene distributions. Real-world deployment on a Unitree Go2 further achieves an 82% success rate over 50 trials, supporting the practical feasibility and sim-to-real transfer of LANav.

---

## 论文详细总结（自动生成）

好的，以下是对论文《Beyond Transformers: Linear Attention Policy for Open-Vocabulary Object Goal Navigation》的详细中文总结。

---

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在**开放词汇物体目标导航**（OVON）任务中，智能体需要在部分可观测的未知环境中，根据语言描述（甚至是从未见过的类别）找到目标物体。这要求策略网络能有效地整合历史观测信息，进行**内部状态更新**。然而，当前主流的基于Transformer的策略（使用自注意力机制）存在一个反直觉的现象：增加历史观察的上下文窗口长度，并不能提升导航性能。这表明自注意力机制可能不适合处理导航问题中状态整合的需求。
- **研究动机**：作者旨在探索一种比Transformer自注意力更有效的状态更新机制，以提升OVON策略在长时序、高噪声（大量无关观测）环境下的决策能力。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：放弃使用自注意力来处理一整段历史上下文，转而采用**线性注意力**（Linear Attention, LA）作为策略网络的主干。线性注意力通过**结构化、增量式的状态更新**来压缩和整合历史信息，模拟了一个更高效的“记忆”过程。
- **关键技术细节**：
    1.  **LANav（Linear Attention-based Navigation）架构**：
        - 输入编码：使用冻结的SigLIP视觉编码器处理RGB图像，SigLIP文本编码器处理目标描述，并使用可学习的嵌入层处理上一个动作。
        - 特征融合：将视觉、语言和动作嵌入拼接并通过线性投影，得到每步的观测嵌入 `õt`。
        - 策略主干：将最近 `l` 步的观测嵌入序列输入到线性注意力（LA）主干中，LA通过**维护一个状态矩阵**来增量式地整合信息，输出当前步的策略表示 `yt`。
        - 动作预测：将策略表示输入动作头，输出离散动作上的概率分布。
    2.  **LA vs. Transformer**：
        - Transformer：使用QKV自注意力，需要存储所有历史token，计算复杂度为 `O(l²)`（l为上下文长度）。历史信息通过token之间的显式交互获取。
        - LA：将历史信息压缩到一个状态矩阵 `S_t` 中，通过“写入”和“读取”操作实现。每一步只需处理当前token，复杂度为 `O(l)`。历史信息通过**结构化的状态更新**获取。
    3.  **WSLA（Weighted State-Expansion Linear Attention）**：
        - 动机：进一步提升LA的状态表达能力。
        - **状态扩展**：将每个注意力头的单个状态 `S(h)` 扩展为 `E` 个独立的子状态 `S(h,e)`，为每个子状态配备独立的查询和键投影。
        - **加权读出**：不同子状态可能捕捉到历史信息的不同侧面。WSLA引入一个**可学习的加权向量 `λ`**，通过softmax归一化来聚合所有子状态的输出，赋予不同子状态不同的重要性，从而更精细地控制信息的读取。

### 3. 实验设计：数据集、Benchmark、对比方法

- **数据集与Benchmark**：
    - **主数据集**：HM3D-OVON，它提供了三个用于评估语义泛化能力的验证集，总体构成一个严谨的基准：
        - `VAL SEEN`: 包含训练中见过的目标类别。
        - `VAL SEEN SYN`: 包含训练中未见过的、但在语义上高度相关的近义词。
        - `VAL UNSEEN`: 包含语义上截然不同的全新类别。
    - **迁移数据集**：HSSD（Habitat Synthetic Scenes Dataset）ObjectNav benchmark，用于验证模型对不同场景分布的鲁棒性。
- **对比方法**：
    - **RNN基线**：GRU
    - **Transformer基线**：标准Transformer（LLaMA式）、DAgRL（原始方法）、EALM（一个更复杂的SOTA方法）。
    - **LANav内部的LA变体对比**：Linear Transformer、DeltaNet、Gated DeltaNet，以及作者提出的WSLA。

### 4. 资源与算力

- 论文明确说明了训练细节：
    - **GPU型号与数量**：8块 NVIDIA A100 80GB GPU。
    - **训练流程**：两阶段训练。第一阶段使用DAgger进行150M步的预训练，第二阶段使用PPO进行100M步的微调，总计250M训练步。
    - **并行设置**：每块GPU并行运行32个环境，共计256个并行环境。

### 5. 实验数量与充分性

论文的实验设计较为充分和客观，主要包括：
1.  **主实验结果**：表格1展示了在HM3D-OVON的三个验证集上，LANav的所有变体与所有基线方法的SR和SPL对比。
2.  **上下文长度影响分析**：图2系统性地探究了不同训练和测试上下文长度下，Transformer与LA策略的性能变化趋势。
3.  **状态更新机制分析**：通过对比不同LA变体的性能（表1），逐步揭示了状态更新规则设计的重要性。
4.  **距离分层评估**：表2将测试episode按初始距离分组，分析了WSLA在不同难度场景下的相对收益。
5.  **消融实验**：表3对WSLA的两个关键组件（状态扩展和加权）进行了消融。
6.  **效率分析**：图4对比了不同模型的计算复杂度和参数数量。
7.  **泛化与适应实验**：表4在HSSD数据集上进行了零样本迁移和微调实验，验证了WSLA的鲁棒性。
8.  **真实世界验证**：在Unitree Go2机器人上进行了50次试验，验证了算法的实际可行性。
这些实验从多个维度（性能、效率、泛化、消融、实机验证）验证了方法的有效性，且所有对比均在**公平、可控的设置下进行**，具有充分的客观性。

### 6. 论文的主要结论与发现

1.  **LA优于RNN和Transformer**：在相同的架构和训练设置下，基于线性注意力的策略网络（LANav）在OVON任务上始终优于传统RNN和Transformer基线。
2.  **LA受益于长上下文**：与Transformer不同，LA模型能从更长的训练上下文长度中获益，并且在不同测试上下文长度下表现稳定。
3.  **状态更新设计是关键**：性能与状态更新机制的“结构化”和“规范化”程度正相关（Linear Transformer < DeltaNet < Gated DeltaNet < WSLA）。简单地使用线性公式替代自注意力并不足够，关键在于如何设计“写入”、“保留”和“读取”历史信息的过程。
4.  **WSLA有效提升性能**：通过状态扩展和加权读出，WSLA实现了最优的性能，在HM3D-OVON上平均SR达到36.4%，显著超越最好的Transformer基线（30.1%）。长距离导航场景下的性能提升尤为明显。
5.  **计算效率高**：LA策略的计算量不随上下文长度增长而增加，WSLA在保持高效率的同时取得了最佳效果，实现了效能和效率的良好平衡。
6.  **具备Sim-to-Real迁移能力**：在HSSD数据集上表现出鲁棒性，并在真实机器人上实现了82%的成功率，证明了其实用价值。

### 7. 优点：方法或实验设计上的亮点

- **方法上的亮点**：
    - **问题洞察深刻**：在微小的“性能不随上下文长度增长”观察背后，发现了自注意力机制在导航状态更新中的根本性缺陷，并提出了一个优雅的替代方案。
    - **WSLA设计简洁而有效**：状态扩展和加权读出是两个直观且可解释的改进，能显著提升线性注意力的表达能力，且计算开销增加不多。
    - **系统性地探讨了状态更新设计**：通过对比不同LA变体，将研究焦点从“用什么架构”提升到“如何设计状态更新规则”这一更本质的层面。
- **实验设计上的亮点**：
    - **公平且严谨的控制实验**：整个实验设计（DAgRL框架）唯一变化的变量就是策略网络本身，确保了比较的公平性和结论的可靠性。
    - **全面的分析实验**：不仅报告了最终性能，还深入探究了上下文长度影响、状态更新机制、距离分层收益等，提供了丰富的洞察。
    - **覆盖全面的评估**：包括了仿真环境的标准Benchmark、跨场景泛化、计算效率分析，以及最终的真实世界机器人验证，使得研究成果具有很强的说服力。

### 8. 不足与局限

- **模型规模有限**：论文自承模型使用了较浅的LA网络（4层），上下文窗口大小也有上限，未来的工作可以考虑扩展模型容量和上下文范围，探索更大规模模型下的性能。
- **动作空间限制**：真实世界验证中仅使用了4个动作（前进、左转、右转、停止），未涉及上下环顾等动作，这降低了任务的复杂度。虽然不影响核心结论，但离完全自主的机器人还有距离。
- **仿真环境的简化**：HM3D-OVON数据集提供了良好的基准，但仿真环境与现实世界的复杂性（如动态物体、光照变化、传感器噪声等）仍有差距。虽然进行了实机验证，但50次试验的规模相对较小，环境也较为可控。
- **未与其他SOTA地图方法对比**：论文的对比主要集中于“无地图”策略网络（mapless policy）。虽然这有助于聚焦研究核心问题，但未与当前同样具有竞争力的基于地图的方法（如用到VLM、检测器等方法）进行直接对比。这些方法在模块构成、训练协议、传感器使用上存在巨大差异，进行公平比较本身非常困难，但作为局限仍需提及。

（完）
