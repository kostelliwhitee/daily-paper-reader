---
title: "The Entropic Bound for Transformers: Why Static Rank Fails and Attention-Native Rank Recovers"
title_zh: Transformer的熵界：为什么静态秩失效而注意力原生秩恢复
authors: Byeong Hoon Yoon
date: 2026-07-25
pdf: "https://arxiv.org/pdf/2607.23050v1"
tags: ["query:ma-la"]
score: 8.0
evidence: 线性注意力代理用于理论分析
tldr: 神经缩放定律未回答固定任务所需最小模型容量。本文提出基于熵界的任务内在容量度量，在线性注意力代理中证明令牌混合算子内在秩是紧下界且梯度下降可恢复。静态权重秩在真实注意力中失败，定义注意力原生内在秩（最小查询键核秩）后恢复缺陷性、可达性和可恢复性。最后映射数据可预测性边界，将熵界重新定义为注意力原生容量度量，具有精确的可预测性前沿。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23050v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1612, \"height\": 244, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23050v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 689, \"height\": 359, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23050v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1343, \"height\": 318, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23050v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1057, \"height\": 637, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23050v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1255, \"height\": 638, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23050v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1389, \"height\": 233, \"label\": \"Table\"}]"
motivation: 现有缩放定律不回答固定任务所需最小模型容量，本文通过熵界研究Transformer内在容量下限。
method: 在线性注意力代理中证明令牌混合算子内在秩为紧下界，并定义注意力原生内在秩（最小查询键核秩）来适配真实注意力。
result: 注意力原生内在秩恢复熵界结构，并揭示线性QK注意力可精确恢复秩，softmax仅部分恢复的预测性边界。
conclusion: 重新将熵界定义为注意力原生容量度量，为模型容量设计提供理论依据。
---

## 摘要
神经缩放定律描述了随着模型、数据和计算量的增长，损失如何下降，但它们并未回答一个先决问题：对于固定任务，解决该任务所需的最小模型容量是多少？我们通过熵界——一种面向Transformer的任务内在容量的谱概念——来研究这一问题。我们首先证明，在线性注意力替代模型中，token混合算子的内在秩$r^*$是一个紧下界：任何秩不足的模型都会招致不可避免的额外风险，且该界在$r^*$处可达。我们进一步证明，梯度下降在标准的低秩隐式偏差假设下能恢复该秩，通过实验确认了所有三个性质，并展示了$r^*$可在训练前从数据中恢复。然后我们探究这是否能迁移到真实注意力上。简单迁移失败，一个受控的插值阶梯精确定位了原因：不是softmax，也不是秩约束，而是注意力混合算子的输入条件化本质——静态权重核无法概括。受此启发，我们引入了注意力原生内在秩——在注意力类中实现任务所需的最小查询-键核秩——并证明在此定义下，完整的熵界结构（不足性、可达性、恢复性）在线性和softmax注意力中均得以恢复，且能量有效秩是对softmax失真鲁棒的估计量。最后，我们描绘了仅数据可预测性的边界：对于线性QK注意力，$r^*$可精确恢复，即使在没有值映射的情况下；而softmax注意力由于非线性反演和核-值可识别性效应，仅允许部分预训练恢复。我们的结果将熵界从事后描述符重新构建为一种注意力原生容量度量，并具有精确刻画的可预测性边界。

## Abstract
Neural scaling laws describe how loss decreases as models, data, and compute grow, but they do not answer a prior question: for a fixed task, what is the minimum model capacity required to solve it? We study this through the Entropic Bound, a spectral notion of task-intrinsic capacity for Transformers. We first prove that, in a linear attention surrogate, the intrinsic rank $r^*$ of the token-mixing operator is a tight lower bound: any rank-deficient model incurs unavoidable excess risk, and the bound is achievable at $r^*$. We further show that gradient descent recovers this rank under standard low-rank implicit-bias assumptions, confirm all three properties empirically, and show $r^*$ is recoverable from data before training. We then ask whether this transfers to real attention. A naive transfer fails, and a controlled interpolation ladder localizes the cause precisely: it is not softmax and not a rank constraint, but the input-conditioned nature of attention's mixing operator, which a static weight kernel cannot summarize. Motivated by this, we introduce an attention-native intrinsic rank -- the minimum query-key kernel rank realizing the task within the attention class -- and show that under this definition the full Entropic Bound structure (deficiency, achievability, recovery) is restored for both linear and softmax attention, with the energy effective rank as the estimator robust to softmax distortion. Finally, we map the boundary of data-only predictability: $r^*$ is exactly recoverable for linear QK attention, even without the value map at scale, while softmax attention admits only partial pre-training recovery due to nonlinear inversion and kernel-value identifiability effects. Our results reframe the Entropic Bound from a post-hoc descriptor into an attention-native capacity measure with a precisely characterized predictability frontier.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有神经缩放定律仅描述损失随模型、数据、计算量增长的轨迹，但未回答一个根本问题——**对于固定任务，所需的最小模型容量是多少**。作者试图寻找一种任务内在的容量度量，即“熵界”（Entropic Bound），以刻画Transformer解决给定任务所需的最低有效维度。
- **核心含义**：论文将缩放定律从“后验描述”转向“先验约束”，为模型容量设计提供理论下限。通过引入**注意力原生内在秩**，重新定义了Transformer的容量度量，并精确刻画了其在不同注意力变体下的预测性与局限性。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：任务内在容量由令牌混合算子的谱结构决定。在线性注意力代理（固定算子 \(Y = A^*XV^*\)）中，最小所需秩 \(r^*\) 是紧下界。
- **关键技术细节**：
  - **线性代理中的紧界**：提出了三个定理：
    - **不足性**：若模型秩 \(r < r^*\)，则必然存在不可忽略的额外风险；
    - **可达性**：在 \(r = r^*\) 时，模型可精确达到噪声下界；
    - **梯度下降恢复性**：从极小初始化出发，梯度下降会收敛到有效秩为 \(r^*\)（有限样本下含Marchenko–Pastur修正）。
  - **注意力原生定义**：为避免静态核失效，定义注意力原生内在秩为：  
    \[
    r^*_{\text{attn}}(\varepsilon; \mathcal{D}) = \min\{\operatorname{rank}(K): \exists V, \mathbb{E}\|Y - \operatorname{Attn}_{K,V}(X)\|^2 \leq \varepsilon\}
    \]  
    教师任务从同一注意力类生成（线性QK或softmax QK）。
  - **能量有效秩**：使用能量有效秩 \(\tilde{R}_{\text{eff}}\)（基于奇异值平方的归一化熵）作为对softmax失真鲁棒的容量估计量。
- **算法流程**：通过解耦的插值阶梯（B0→B1→B2→B3）定位失败原因：  
  - B0: 固定算子 \(AXV\) → 成功  
  - B1: 加入QK因子化 \(XKX^\top XV\) → 失败（输入条件化导致）  
  - B1c: 全秩 \(K\) → 同样失败（排除秩约束原因）  
  - B2/B3: 加入softmax、残差等 → 仍失败  
  结论：失败根源是**输入条件化混合**，非softmax或秩本身。

### 3. 实验设计：使用的数据集/场景、基准、对比方法

- **主要实验场景**：合成教师-学生任务，固定序列长度 \(T=16\)，模型维度 \(d=32\)，\(r^* \in \{1,2,3,4,6,8\}\)，噪声 \(\sigma=0.05\)，\(n=3000\)样本（附录C）。
- **基准**：噪声下界 \(\sigma^2 T d\)。
- **对比方法**：
  - 线性代理（B0）与注意力变体（B1、B2、B3）的插值对比；
  - 线性QK教师 vs softmax QK教师；
  - 谱间隙消融（sharp / power-law / flat）；
  - 数据可预测性对比：数值秩、能量有效秩（核侧、实现混合器侧）、V已知/未知。
- **辅助实验**：在真实语料（英文散文、莎士比亚、Python源码、Linux C源码、JSON、ABC音乐符号）上检验静态秩的失效，验证测量秩与任务复杂度反相关。

### 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量、训练时长。附录C提到优化器为Adam，学习率 \(10^{-2}\)，训练步数1500~2500步。由于是单机小规模合成实验，推测算力需求较低（可在普通GPU或CPU上完成）。
- 真实语料实验也仅为特征分析，未进行大规模训练。

### 5. 实验数量与充分性

- **数量**：包含6组主要表格实验：
  - 表1：线性代理验证（秩1~6，\(r^*=3\)）；
  - 表2：插值阶梯（B0~B3，含B1c全秩控制）；
  - 表3：注意力原生内在秩全网格（线性/softmax教师 × 6种 \(r^*\)，5种子）；
  - 表4：数据可预测性（线性/softmax教师，V已知/未知，核侧/实现侧）；
  - 表5：谱间隙消融（sharp/power/flat，\(r^*=4\)）；
  - 附录B：真实语料诊断（6个语料 × 3种模型维度 × 5种子）。
- **充分性**：实验覆盖了理论核心的所有断言（不足性、可达性、恢复性、可预测性），消融对照充分，种子稳定（CV≤0.003）。样本规模偏小（\(n=3000\)），但合成数据足够验证性质。**未进行大规模自然语言任务的验证**，这是主要局限。
- **公平性**：教师-学生结构严格匹配，超参数一致，对比基线合理，未发现不公平设计。

### 6. 论文的主要结论与发现

1. **线性代理中熵界是紧的**：秩不足导致额外风险，秩 \(r^*\) 可达噪声下界，梯度下降可恢复 \(r^*\)。
2. **静态秩迁移失败**：失败根源是注意力的输入条件化混合，而非softmax或秩约束。
3. **注意力原生内在秩恢复完整结构**：在注意力类内定义后，不足性、可达性、恢复性在线性QK和softmax注意力中均成立，且能量有效秩为鲁棒估计量。
4. **可预测性边界**：
   - 线性QK注意力：可从数据精确恢复 \(r^*\)（包括V未知情况，当维度足够大时）；
   - softmax注意力：仅部分恢复（数值秩饱和，核侧能量有效秩在低\(r^*\)过估，实现混合器侧在低\(r^*\)压缩）。
5. **真实语言任务尚未穿透**：当前语言模型的实现混合器被位置、注意力集中等先验主导，无法直接分离任务内在秩。

### 7. 优点：方法或实验设计上的亮点

- **理论紧致性**：给出了完整的“不足性-可达性-恢复性”三性质，并将传统Eckart–Young–Mirsky定理与动态恢复结合。
- **精确定位失败原因**：通过插值阶梯设计（B0→B1→B1c→B2→B3），排除了softmax和秩约束的干扰，直指输入条件化本质。
- **定义转移**：从静态核转向注意力类内在定义，消解了架构不匹配问题，使理论对真实注意力有效。
- **谱消融全面**：检查了sharp、power-law、flat三种谱轮廓，证明能量有效秩是普适度量，而非依赖谱间隙的伪影。
- **可预测性边界**：诚实报告softmax下的部分可预测性，并指出“寻找单一预训练估计量”为开放问题，避免过度承诺。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **实验环境局限**：所有正面结果均基于合成教师-学生任务，序列长度仅16，维度仅32，未在长序列、大规模模型上验证。
- **真实语言任务未解决**：附录B详细论证了当前语言模型的实现算子被非任务结构主导，因此论文的注意力原生秩无法直接迁移。这是最重要的局限性。
- **softmax可预测性不完整**：无单一预训练估计量能精确恢复所有\(r^*\)，仅提供部分刻画。
- **理论假设较强**：线性代理的证明依赖全秩输入协方差、谱间隙等条件，实际场景可能不满足。
- **梯度下降恢复的理论仅提供证明梗概**：有限样本、有限宽度下的严格分析留给未来。
- **工程应用距离较远**：论文未提供如何将注意力原生秩用于模型剪枝、缩放策略等实际方案。

（完）
