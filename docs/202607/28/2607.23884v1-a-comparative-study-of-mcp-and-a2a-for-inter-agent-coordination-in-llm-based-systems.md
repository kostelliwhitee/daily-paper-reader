---
title: A Comparative Study of MCP and A2A for Inter-Agent Coordination in LLM-Based Systems
title_zh: 基于LLM的系统中MCP与A2A的智能体间协调比较研究
authors: "Ionut Predoaia, Tuong Manh Vu, Konstantinos Barmpis, Dimitris Kolovos, Antonio García-Domínguez"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23884v1"
tags: ["query:ma-la"]
score: 8.0
evidence: 比较多智能体系统中MCP和A2A的协调协议
tldr: LLM多智能体系统的快速发展对智能体间协调协议提出新需求。本文以相同软件工程任务为场景，对比实现MCP与A2A两种协议，评估发现MCP以轻量实现支持协调但需显式处理状态管理，而A2A通过协议级抽象原生支持有状态多轮对话但复杂度显著更高。研究揭示不同协议抽象如何影响协调责任分配，为工程决策提供实证参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23884v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1236, \"height\": 842, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23884v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1232, \"height\": 840, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23884v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1154, \"height\": 697, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23884v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 675, \"height\": 599, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23884v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 525, \"height\": 611, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23884v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 661, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23884v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 845, \"height\": 325, \"label\": \"Table\"}]"
motivation: 现有LLM多智能体系统缺乏统一的协调协议，需比较MCP与A2A在实际任务中的优劣。
method: 针对同一软件工程任务，分别基于MCP和A2A实现多智能体系统，对比其发现性、多轮对话、异步通信等能力。
result: MCP实现更轻量但需应用层处理对话状态；A2A原生支持状态管理但实现与协调复杂度显著更高。
conclusion: 协议抽象决定协调职责分配，MCP适合简单场景，A2A适合需复杂状态管理的系统。
---

## 摘要
近期行业实践中，由异构的、工具和LLM中介的智能体组件构成的智能体系统迅速涌现，引发了关于智能体间协调和协议设计的实际问题。本文从多智能体系统工程视角出发，基于模型上下文协议和智能体间协议的实现，通过一个涉及基于LLM的智能体的智能体间协调场景进行了比较。我们针对同一软件工程任务，分别实现了基于MCP和基于A2A的多智能体方案，并根据先前文献及与行业伙伴讨论得出的一组需求进行评估，包括智能体可发现性、多部分消息、多轮对话、异步通信、可观测性、互操作性和访问控制。结果表明，MCP通过相对轻量级的实现模型和较低的协调复杂度，能够支持受限的基于LLM的系统中的智能体间协调，尽管会话状态管理和任务生命周期处理等协调问题必须在应用层显式实现。相比之下，A2A通过协议层面的任务和生命周期管理抽象，为有状态的多轮协调提供了更丰富的原生支持，但这带来了显著更高的实现和协调复杂度。鉴于所评估协调模式的有限范围，这些发现作为实证经验报告中的设计观察提出，而非关于协议在更广泛MAS中的适用性或优越性的一般性断言，重点突出了权衡取舍以及协议抽象如何塑造当代智能体系统中协调职责的分布。

## Abstract
Recent industry practice has seen the rapid emergence of agentic systems composed of heterogeneous, tool- and LLM-mediated agent components, raising practical questions about inter-agent coordination and protocol design. This paper presents an implementation-grounded comparison of the Model Context Protocol (MCP) and the Agent2Agent (A2A) protocol, from a multi-agent systems engineering perspective, using an inter-agent coordination scenario involving LLM-based agents. We evaluate an MCP-based and an A2A-based multi-agent implementation of the same software engineering task against a set of requirements derived from prior literature and discussions with industry partners, including agent discoverability, multi-part messaging, multi-turn conversations, asynchronous communication, observability, interoperability, and access control. The results evidence that MCP can support inter-agent coordination in constrained LLM-based systems through a comparatively lightweight implementation model with lower coordination complexity, although coordination concerns such as conversational state management and task lifecycle handling must be implemented explicitly at the application layer. In contrast, A2A provides richer native support for stateful, multi-turn coordination through protocol-level abstractions for tasks and lifecycle management, but this comes with substantially greater implementation and coordination complexity. Given the narrow scope of the evaluated coordination pattern, these findings are presented as design observations from an empirical experience report rather than general claims of protocol suitability or superiority across broader classes of MAS, highlighting trade-offs and how protocol abstractions shape the distribution of coordination responsibilities in contemporary agentic systems.

---

## 论文详细总结（自动生成）

# 基于LLM的系统中MCP与A2A智能体间协调比较研究——详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：近年来，基于大语言模型（LLM）的智能体系统快速涌现，智能体之间需要协作完成复杂任务。智能体间协调协议的设计成为关键工程问题。两种主流协议——Model Context Protocol（MCP，原为工具和资源访问设计）和Agent2Agent（A2A，专为智能体间通信设计）——在行业中备受关注，但缺乏直接的实证比较。
- **核心问题**：在已有的LLM系统中，如果已采用MCP进行工具集成，是否可复用MCP来支持智能体间协调？还是需要引入专为智能体间通信设计的A2A？两种协议在实现同一协调任务时，其实施复杂度和协调职责分布有何差异？
- **整体含义**：通过实现同一软件工程任务（从自然语言描述生成面向对象领域模型），对比MCP和A2A在发现性、多部分消息、多轮对话、异步通信、可观测性、互操作性、访问控制等七个需求上的支持程度，揭示协议抽象如何影响协调责任的分配，为工程决策提供实证参考。

## 2. 方法论

- **核心思想**：采用实现驱动的比较研究方法，针对同一协调模式（任务委派-迭代反馈-验证）分别开发MCP和A2A两类多智能体系统，保持协作算法和智能体内部逻辑不变，仅替换通信协议，从而隔离协议层面的影响。
- **关键技术细节**：
  - **协作算法（Algorithm 1）**：由协作Agent协调，包含：
    1. 接收用户提示。
    2. 将提示交给解决方案Agent（使用LLM生成Emfatic格式的领域模型）。
    3. 将生成的模型交给语法监督Agent（Java编写的Emfatic解析器，检验语法正确性）。
    4. 如语法错误，反馈给解决方案Agent修正，最多N次尝试。
    5. 通过语法验证后，交给语义监督Agent（LLM通过少样本提示检查模型是否满足需求）。
    6. 如语义错误，反馈修正；否则输出最终模型。
  - **MCP实现**：每个Agent暴露为一个MCP Server，其行为封装为一个tool。用户通过MCP Client调用协作Agent的tool，协作Agent内部再通过MCP Client调用其他Agent的tool。通信遵循JSON-RPC，使用SSE或Streamable HTTP进行异步/流式传输。
  - **A2A实现**：每个Agent作为A2A Server（通过HTTP/JSON-RPC），使用Agent Card描述能力。协作Agent通过创建Task（带唯一ID和context ID）来发起工作，利用Task状态（pending、working、input-required、completed、failed等）和Artifact更新事件传递中间结果。支持流式更新和长时间任务生命周期管理。
- **公式或算法流程（文字说明）**：见Algorithm 1伪代码，无公式。

## 3. 实验设计

- **使用的任务/场景**：软件工程中的“从自然语言生成面向对象领域模型”任务，模型用Emfatic语言表示。包含30个自然语言提示：10个简单，20个复杂（示例：图书馆管理系统）。
- **基准（Benchmark）**：无标准基准，本质上是两个协议实现的分项对比。
- **对比的方法**：仅对比MCP和A2A两种实现，无第三方方法或基线。比较维度包括：
  - 实现复杂度（代码行数、协调原语数、协调阶段数）。
  - 需求满足度（7个需求，满分/部分满足/不满足）。
  - 使用的LLM同为`qwen2.5-coder:32b`。
- **评估方式**：
  - 代码行数统计：使用`cloc`工具（排除注释和空行）。
  - 协调原语和阶段：手动分析代码和协议规范。
  - 需求满足度：基于实际实现能力（R3多轮对话A2A原生支持，MCP需要应用层实现；R5可观测性两者均需第三方库集成）。

## 4. 资源与算力

- 论文**未明确说明**使用的硬件资源（GPU型号、数量、训练时长等）。仅在实现中提到使用了`qwen2.5-coder:32b` LLM，推测为本地运行，但具体推理环境未披露。
- 实验均为协议实现对比，不涉及大模型训练，仅需推理。但论文没有记录推理时的计算开销（如时间、token消耗），因此算力需求不可知。

## 5. 实验数量与充分性

- **实验数量**：不多。两组实现在同一组30个提示上运行（验证了可生成正确模型），未做大规模统计或性能测试。未做消融实验（如改变重试次数N、不同LLM、不同协调模式）。
- **充分性与公平性**：
  - **充分性不足**：仅一个协调模式（任务委派迭代验证），不能推广到其他模式（如竞标、广播、共享黑板）。未测试动态协调或容错场景。
  - **公平性较好**：保持算法和LLM一致，仅协议不同；源码公开（OSF存储库），可复现。
  - **偏差风险**：作者自身实现可能倾向MCP（文中显示MCP代码更小），但结论指出了两者的优缺点，无明显偏向。

## 6. 主要结论与发现

- MCP可以支持智能体间协调，但**需要应用层显式实现**对话状态管理、任务生命周期跟踪等功能，实现模型**轻量**（1255行代码，4个协调原语，每交互2个协调阶段）。
- A2A原生支持有状态多轮协调（通过Task状态机、input-required等），但实现**更复杂**（1898行代码，10个协调原语，每交互6个协调阶段）。
- 在需求满足度上，A2A全满足7个需求中的6项（部分满足1项：可观测性），MCP满足5项，未满足多轮对话（R3），部分满足可观测性。
- 协议抽象决定了协调职责的分配：MCP的设计假定协调逻辑在应用层，A2A将协调逻辑提升到协议层。
- 建议：选择协议应基于对状态管理和实施复杂度的权衡，而非简单优劣比较。这些发现是“设计观察”而非普遍性结论。

## 7. 优点

- **方法创新**：首次在实现层面对MCP和A2A进行直接比较，填补了文献空白。
- **结构化评估**：基于文献和行业需求提取7个维度，评估全面且具现实意义。
- **公平性保障**：保持智能体内部逻辑、LLM、任务定义完全一致，仅替换通信层。
- **透明度**：所有代码和数据已公开（OSF），支持复现和扩展。
- **实践导向**：结果直接回答工程实践中的常见问题（是否能用MCP替代A2A做协调）。

## 8. 不足与局限

- **实验覆盖**：仅评估一种协调模式（委托-验证），结论不可推广到其他模式（如协商、投票、市场）。
- **未测试性能**：无端到端延迟、吞吐量、错误率等定量指标；未对比不同规模LLM下的表现。
- **未考虑可扩展性**：实验中仅包含4个Agent，未测试大规模Agent群组下的协议表现。
- **缺少消融研究**：未分析不同重试次数、提示复杂度、LLM替换对协议需求的影响。
- **人工因素**：实现由作者团队开发，可能带有偏好；代码复杂度统计可能受编程风格影响。
- **忽略安全与合规细节**：访问控制仅描述协议机制，未实际测试授权/认证场景。
- **未涉及动态协调**：协议在运行时合成协调结构的能力未研究（列为未来工作）。

（完）
