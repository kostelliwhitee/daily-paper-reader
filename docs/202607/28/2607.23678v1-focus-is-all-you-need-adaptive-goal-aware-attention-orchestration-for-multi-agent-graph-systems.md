---
title: "Focus Is All You Need: Adaptive Goal-aware Attention Orchestration for Multi-Agent Graph Systems"
title_zh: 聚焦即所需：面向多智能体图系统的自适应目标感知注意力编排
authors: "Mingzhou Fan, Siyuan Xu, Mingxuan Yuan"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23678v1"
tags: ["query:ma-la"]
score: 9.0
evidence: 提出注意力编排范式，将Transformer注意力扩展到多智能体图系统中的工作流级协调
tldr: 多智能体图系统因均匀执行导致资源浪费。提出自适应目标感知注意力编排（AGAO），将Transformer式注意力从token扩展到工作流级智能体协调，通过目标感知、拓扑感知和资源感知三种注意力动态评估智能体重要性并分配预算。实验表明，AGAO在多种任务上提升效果，同时降低延迟和令牌消耗。该工作开创了注意力工程方向，为可扩展智能多智能体系统奠定基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23678v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1432, \"height\": 454, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23678v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1361, \"height\": 923, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23678v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1400, \"height\": 244, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23678v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1455, \"height\": 286, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23678v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 981, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23678v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 875, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23678v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1059, \"height\": 304, \"label\": \"Table\"}]"
motivation: 现有图编排方法均匀执行所有组件，忽略任务目标差异，造成大量计算和令牌浪费。
method: 提出AGAO框架，结合目标感知、拓扑感知和资源感知注意力，动态评估智能体重要性并分配资源。
result: 在多种多智能体工作负载上，AGAO提升任务效果，降低不必要的计算、延迟和令牌消耗。
conclusion: 本工作将注意力思想扩展到工作流级智能体协调，开创注意力工程方向，推动可扩展多智能体系统。
---

## 摘要
大型语言模型（LLMs）使自主智能体能够进行推理、规划和工具使用。最近的系统越来越多地将这些智能体组织为专门的、相互连接的节点图。尽管基于图的编排支持灵活的分解和协调，但它带来了一个关键挑战：**注意力分配**。随着工作流的增长，现有方法通常均匀地执行图组件，在无关或低影响的任务上浪费资源。我们引入了**注意力编排**，这是一种将Transformer风格的注意力从令牌表示扩展到工作流级智能体协调的范式。我们的框架**自适应目标感知注意力编排（AGAO）**根据用户目标、图依赖关系和计算约束动态估计智能体的重要性。AGAO结合了三个组件：（1）目标感知注意力，衡量用户目标与智能体能力之间的语义相关性；（2）拓扑感知注意力，建模智能体图中的结构依赖关系；（3）资源感知注意力，跨异构智能体分配预算和执行优先级。这些机制共同将静态智能体图转化为自适应系统，将计算集中在目标关键推理路径上。在多种多智能体工作负载上的实验表明，与现有的基于图的执行策略相比，AGAO提高了任务效果，同时减少了不必要的计算、延迟和令牌消耗。我们的工作将**注意力工程**确立为可扩展、智能多智能体系统的一个方向。代码：https://github.com/MingzhouFan97/AGAO。

## Abstract
Large language models (LLMs) enable autonomous agents for reasoning, planning, and tool use. Recent systems increasingly organize these agents as graphs of specialized, interconnected nodes. Although graph-based orchestration supports flexible decomposition and coordination, it creates a key challenge: \textbf{attention allocation}. As workflows grow, existing approaches often execute graph components uniformly, wasting resources on irrelevant or low-impact tasks. We introduce \textbf{Attention Orchestration}, a paradigm that extends Transformer-style attention from token representations to workflow-level agent coordination. Our framework, \textbf{Adaptive Goal-aware Attention Orchestration (AGAO)}, dynamically estimates agent importance based on user objectives, graph dependencies, and computational constraints. AGAO combines three components: (1) goal-aware attention, measuring semantic relevance between user goals and agent capabilities; (2) topology-aware attention, modeling structural dependencies in agent graphs; and (3) resource-aware attention, allocating budgets and execution priorities across heterogeneous agents. Together, these mechanisms transform static agent graphs into adaptive systems that focus computation on goal-critical reasoning paths. Experiments across diverse multi-agent workloads show that AGAO improves task effectiveness while reducing unnecessary computation, latency, and token consumption compared with existing graph-based execution strategies. Our work establishes \textbf{Attention Engineering} as a direction for scalable, intelligent multi-agent systems. Code: https://github.com/MingzhouFan97/AGAO.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有基于图的多智能体系统（如 LangGraph、AutoGen 等）虽然能够通过有向图来组织和协调多个专业智能体，但存在一个根本缺陷——**注意力稀释**：当图节点规模扩大时，系统往往均匀地执行所有可达节点，导致大量计算资源浪费在低相关或低影响的任务上。换句话说，当前系统能知道“哪些任务存在以及如何连接”，但不能决定“哪些任务在当前时刻值得关注”。
- **整体含义**：作者认为，未来的智能体系统不仅需要推理能力，还需要动态分配计算焦点的能力。受 Transformer 中注意力机制的启发，他们将注意力从“token 级表示学习”扩展到“工作流级智能体协调”，提出一种新的范式——**注意力工程（Attention Engineering）**，并具体实现了 **AGAO** 框架，旨在将静态智能体图转化为自适应执行系统，只聚焦于目标关键的推理路径。

### 2. 论文提出的方法论

- **核心思想**：AGAO 在用户目标与智能体图执行之间引入一个注意力驱动的编排层，该层三个互补组件协同工作，动态评估每个智能体的重要性，并据此分配计算资源、执行优先级和模型容量。
- **关键技术细节**：
  - **(1) 目标感知注意力**：将用户目标编码为查询向量 \(Q_g\)，每个智能体节点构建包含能力描述、历史执行信息、当前状态的表示 \(z_i\)，投影为键 \(K_i\) 和值 \(V_i\)。计算注意力分数 \(e_i = Q_g K_i^T / \sqrt{d}\)，经 softmax 归一化得到 \(\alpha_i^{\text{goal}}\)。该分数衡量智能体对目标的语义贡献。
  - **(2) 拓扑感知注意力**：考虑图结构信息（依赖距离、后继影响、关键路径等）。构造图偏置矩阵 \(B_G\)，修改标准自注意力为 \(\text{softmax}(QK^T/\sqrt{d} + B_G)\)。同时引入结构影响分数 \(s_i^{\text{critical}} = \eta_1 d_i^{\text{in}} + \eta_2 d_i^{\text{out}} + \eta_3 c_i^{\text{path}}\)。最终融合语义与拓扑注意力得到 \(\alpha_i^{\text{graph}}\)。
  - **(3) 资源感知注意力**：基于融合后的注意力分数 \(\alpha_i^*\)，进行三种资源分配：
    - 模型路由：根据阈值 \(\tau_h, \tau_l\) 将智能体分配到大型、中型、小型模型；
    - 令牌预算：\(T_i = T_{\text{total}} \frac{\exp(\alpha_i^*)}{\sum_j \exp(\alpha_j^*)}\)；
    - 执行优先级：\(P_i = \alpha_i^* \cdot w_i\)（考虑紧急程度）。
  - **自适应图路由**：在执行步骤 \(t\)，根据中间观察 \(o_t\) 更新注意力状态 \(\alpha^{t+1} = \Phi(\alpha^t, o_t, G, q)\)。基于新注意力分数，将智能体状态分为“执行”“挂起”“剪枝”三类（阈值 \(\tau_e, \tau_s\)），实现动态路径控制。
- **算法流程**（文字描述）：
  1. 初始化状态和注意力分布。
  2. 编码用户目标。
  3. 循环直到任务完成：
     - 计算目标感知注意力和拓扑感知注意力。
     - 融合注意力。
     - 调用资源分配器配置计算资源。
     - 选取注意力高于执行阈值的智能体。
     - 执行选中的智能体。
     - 观察执行反馈。
     - 更新注意力状态和图状态。
  4. 聚合所有输出，返回最终响应。

### 3. 实验设计

- **数据集与场景**：
  - **编程任务**：MBPP（8 个任务），使用 9 节点图（包含规格分析、实现、测试、调试、审查、最终化等角色）。
  - **问答任务**：HotpotQA（8 个多跳问题），使用 9 节点图（包含分解、检索、证据选择、推理、验证、最终化等角色）。
- **基准与对比方法**（共 4 种基线 + AGAO）：
  - **Random**：每次随机选择至多 2 个就绪节点（满足依赖）。
  - **Static**：按固定图顺序执行所有节点。
  - **Semantic**：根据任务提示与节点能力的词法重叠选择至多 2 个节点。
  - **MoE-style**：使用固定的词法相关性+拓扑分数混合，无反馈和自适应资源路由。
- **评价指标**：
  - MBPP：pass@1（通过测试断言）。
  - HotpotQA：token 级 F1（标准化后）。
  - 其他指标：激活节点数、累计智能体执行时间、输入/输出令牌数。

### 4. 资源与算力

- 论文**未明确说明**训练或推理所使用的 GPU 型号、数量以及训练时长。实验是 pilot 评估（每组仅运行一次），且主要报告的是推理阶段的令牌消耗和累计时间，而非训练算力。
- 资源相关数据：AGAO 使用了更多的输入令牌（因路由和上下文构建），但输出令牌显著减少，且在设定的定价下（输出令牌成本为输入令牌的 6 倍）总体更经济。

### 5. 实验数量与充分性

- **实验数量**：两个数据集共 16 个任务（各 8 个），每个任务运行一次。对比了 5 种策略。还进行了消融实验（移除目标、拓扑、资源机制及仅语义变体）。
- **充分性分析**：
  - **优势**：覆盖了编程和问答两种典型智能体任务，消融实验验证了每个模块的必要性。
  - **不足**：每个组合仅一次运行，结果仅为描述性均值，缺乏统计显著性分析（如多次重复取平均和方差检验）。任务规模较小（16 个），且图结构固定为 9 节点，未测试更大规模或更复杂图。因此实验的说服力有限，结果应视为初步验证。

### 6. 论文的主要结论与发现

- **性能提升**：在 MBPP 上，AGAO 的 pass@1 达到 0.875（7/8 任务），优于 MoE-style（0.750）、Static（0.625）等。在 HotpotQA 上，F1 为 0.340，高于大多数基线但低于 MoE-style（0.417）。
- **效率提升**：AGAO 激活节点数最少（MBPP 平均 6.5，HotpotQA 平均 7.25），累计智能体时间分别降低 56% 和 50.3%（相比 Static）。输出令牌大幅减少，在成本上更优。
- **消融结论**：完整 AGAO 提供最好的平衡；单独移除某一机制会引入权衡（例如移除目标感知注意力虽提高 MBPP 但降低 QA 性能并增加时间）。
- **范式意义**：工作确立了“注意力工程”作为智能体系统的新方向，将注意力从表示学习扩展到执行级编排。

### 7. 优点

- **创新性**：首次将 Transformer 式注意力机制系统性地引入多智能体图的工作流级协调，提出注意力编排范式，概念新颖且具有启发性。
- **框架完整性**：AGAO 包含三个互补的注意力组件（目标、拓扑、资源），并支持自适应更新路由，设计相对周密。
- **开销意识**：明确考虑了计算预算、令牌消耗、执行时间等实际约束，并通过资源感知机制动态分配，具有工程实用性。
- **结果具有吸引力**：在有限的 pilot 实验下，展示了在保持或提升任务质量的同时显著降低执行开销的潜力。

### 8. 不足与局限

- **实验规模与统计显著性不足**：仅 16 个任务，每个策略仅运行一次，无法确认结果的稳定性和统计可靠性，存在偶然性风险。
- **图结构固定且规模小**：所有任务使用预定义的 9 节点图，未测试更大规模（数十或数百节点）或动态生成的图，泛化能力未验证。
- **对比基线设置简单**：基线（Random、Static、Semantic、MoE-style）较为基础，未与如 DyLAN、AFlow 等更先进的方法比较（虽然后者侧重不同方面）。
- **目标感知注意力依赖词法相关性**：当前实现使用简单的词法重叠，未使用更复杂的语义编码（如 LLM 嵌入），可能限制了准确性。
- **未充分讨论反馈延迟和并行调度**：在多步自适应路由中，反馈更新带来的额外延迟未被详细分析，累计时间仅为节点执行时间之和，非端到端延迟。
- **缺乏对失败案例的分析**：例如在 HotpotQA 上 AGAO 的 F1 低于 MoE-style，论文未深入解释原因。
- **开放的风险**：过度剪枝可能丢失必要节点，导致任务失败；注意力更新规则基于规则而非学习，可能难以适应多样化的任务动态。

（完）
