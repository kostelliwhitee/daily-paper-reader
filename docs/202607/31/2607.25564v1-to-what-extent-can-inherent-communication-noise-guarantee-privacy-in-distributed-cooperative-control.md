---
title: To What Extent Can Inherent Communication Noise Guarantee Privacy in Distributed Cooperative Control?
title_zh: 固有通信噪声在多大程度上能保证分布式协同控制中的隐私？
authors: "Yuwen Ma, Sarah Spurgeon, Tao Li, Boli Chen"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25564v1"
tags: ["query:ma-la"]
score: 8.0
evidence: 分布式协同控制中的通信噪声隐私模型
tldr: "针对多智能体分布式协同控制中的隐私保护，传统方法需主动注入人工噪声，而实际通信噪声常被忽略。本文提出利用固有通信噪声作为自然隐私机制，建立距离相关的物理噪声模型，并设计有限时域LQR控制器以保护智能体的LQR权重比率。理论证明无需人工噪声即可在无限时域上提供有界累积(ε,δ)-差分隐私保证，同时跟踪误差收敛到有界随机极限。该工作为隐私保护提供了零额外成本的新途径，并量化了隐私与性能的权衡。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25564v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 548, \"height\": 496, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25564v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 901, \"height\": 720, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25564v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 913, \"height\": 726, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25564v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 880, \"height\": 907, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25564v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 250, \"height\": 342, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25564v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 235, \"height\": 344, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25564v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 250, \"height\": 345, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25564v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 236, \"height\": 346, \"label\": \"Figure\"}]"
motivation: 传统方法依赖人工噪声注入保护隐私，但实际通信中固有噪声未被利用；本文探究固有通信噪声能否天然提供隐私保障。
method: 建立含发射机扰动、路径损耗和阴影效应的距离相关噪声模型，设计有限时域LQR机制，保护LQR权重比率而非完整成本函数。
result: "证明无需人工噪声即可对权重比率提供有界累积(ε,δ)-差分隐私，跟踪误差几乎必然均方收敛到有界随机极限。"
conclusion: 为分布式协同控制提供零成本自然隐私保护，并通过数值实验展示了隐私与性能的权衡。
---

## 摘要
本文提出了一种用于多智能体系统（MAS）的差分隐私分布式协同控制方案。与主动注入人工噪声以保护隐私的传统方法不同，本文研究固有通信噪声本身是否可作为一种自然的隐私机制。通过纳入发射器扰动、接收器噪声、路径损耗衰减和对数正态阴影效应，为移动MAS建立了一个物理驱动的通信噪声模型。所得的有效噪声方差依赖于智能体间状态差，从而捕捉了实践中出现的距离相关信号扰动。基于该模型，设计了一种分布式有限时域线性二次型调节器（LQR）机制，在保护智能体私有控制偏好的同时实现编队跟踪。所提出的隐私公式并非保护完整局部代价函数，而是在二次代价结构公开已知时，聚焦于LQR加权矩阵的比率，该比率刻画了跟踪精度与控制代价之间的权衡。集合论敏感性分析表明，在所考虑的添加/删除邻接关系下，这种加权比率邻接公式比基于梯度的保护产生更保守的隐私界。理论分析表明，在适当的设计条件下，所提出的机制无需人工噪声注入即可在无限时域上为加权比率提供有界累积(ε,δ)-差分隐私保证。同时，协同跟踪误差被证明几乎必然且均方收敛到一个有限随机极限，其期望保持有界。数值示例验证了理论结果，并说明了由此产生的隐私-性能权衡。

## Abstract
This paper proposes a differentially private distributed cooperative control scheme for multi-agent systems (MAS). Unlike conventional approaches that actively inject artificial noise for privacy protection, this work investigates whether inherent communication noise can itself serve as a natural privacy mechanism. A physically motivated communication-noise model is developed for mobile MAS by incorporating transmitter perturbation, receiver noise, path-loss attenuation, and log-normal shadowing. The resulting effective noise variance depends on inter-agent state differences, thereby capturing the distance-dependent signal perturbation arising in practice. Based on this model, a distributed finite-horizon Linear Quadratic Regulator (LQR) mechanism is designed to achieve formation tracking while protecting agents' private control preferences. Rather than protecting the full local cost function, the proposed privacy formulation focuses on the ratio of the LQR weighting matrices, which captures the trade-off between tracking accuracy and control effort when the quadratic cost structure is publicly known. A set-theoretic sensitivity analysis shows that this weighting-ratio adjacency formulation yields less conservative privacy bounds than gradient-based protection under the considered addition/removal adjacency relation. Theoretical analysis demonstrates that, under suitable design conditions, the proposed mechanism provides bounded cumulative (ε,δ)-differential privacy guarantees for the weighting ratios over an infinite horizon without artificial noise injection. Meanwhile, the cooperative tracking error is shown to converge almost surely and in mean square to a finite random limit, with its expectation remaining bounded. Numerical examples validate the theoretical results and illustrate the resulting privacy-performance trade-off.

---

## 论文详细总结（自动生成）

# 论文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **隐私保护与分布式控制的冲突**：多智能体系统（MAS）依赖智能体间通信来达成一致性、编队跟踪等协同行为，但通信信号可能被外部窃听者或内部恶意节点利用，以推断智能体的敏感信息（如参考状态、拓扑、代价函数参数或动力学特征）。
- **传统方法的局限**：已有差分隐私（DP）方案大多依赖刻意注入人工噪声（如高斯或拉普拉斯噪声）来掩蔽敏感输出，这种方法虽然有效，但会造成隐私与系统性能之间的固有权衡，且注入噪声大小需按被保护量的敏感度精细校准，扰动过大会恶化收敛精度或闭环性能。
- **本文的核心问题**：无线通信过程中天然存在的物理噪声——发射机畸变、接收机热噪声、路径损耗、遮蔽效应等，能否在不主动注入人工噪声的前提下、直接充当差分隐私保护的自然机制？如果可以，能在多大程度上保证隐私？
- **关键转变**：本文不再关注被动的噪声抑制，而是将固有的通信信道随机性视为一种可利用的隐私资源，探索一条零额外代价的隐私保护路径。
- **隐私保护目标的重新设定**：在LQR/MPC等控制场景中，二次代价函数的结构通常是公开知识，真正需要保护的是编码于**加权矩阵比率（R⁻¹Q）** 中的控制偏好（即智能体在"跟踪精度"与"控制能耗"之间的折中取向）。该比率比完整代价函数或梯度更精炼、更具可保护性。

---

## 2. 论文提出的方法论

### 2.1 核心思想
> **利用物理信道固有的随机性，而非人为注入的噪声，为分布式协同控制中的私密控制偏好提供可证明的（ε,δ）-差分隐私保证。**

### 2.2 通信噪声模型（物理驱动）
- 考虑单输入单输出（SISO）无线信道，发射信号叠加发射机噪声η⁵ 和接收机噪声ηᶻ，经信道增益 h 衰减后到达接收端。
- 信道增益采用经验路径损耗模型：h = C₀·X / ‖xᵢ−xⱼ‖^(α/2)，其中 X 为对数正态遮蔽因子，α 为路径损耗指数，C₀ 为参考距离决定的常量。
- 接收端经相干均衡后，等效噪声的条件分布（给定遮蔽因子）为：
  - ηᵢⱼ | Xᵢⱼ ~ N(0, (zᵢ‖xᵢ−xⱼ‖^α / C₀²Xᵢⱼ² + sⱼ)I)
- **关键特征**：等效噪声方差依赖于智能体间状态差（即距离），体现移动MAS中"距离决定信号扰动"的物理规律。

### 2.3 分布式有限时域LQR机制
- 每个智能体在每个时刻 t 求解一个有限时域（长度 T）的局部最优控制问题，仅施加最优序列的第一个控制量（滚动时域控制）。
- 代价函数被修改为：Jᵢ = Σ [‖uᵢ‖²_Rᵢ + c(t)·‖xᵢ−x̂ᵢⱼ−dᵢⱼ‖²_Qᵢ]，其中 x̂ᵢⱼ 为带噪声的接收状态，c(t) 为趋于零的全局已知时变惩罚因子。
- 控制输入解析表达式：
  - uᵢ(t) = c(t)Kᵢ,ₜ Σⱼ∈Nᵢ aᵢⱼ(x̂ᵢⱼ(t) + dᵢⱼ − xᵢ(t))
  - 增益 Kᵢ,ₜ 由（12）–（13）式闭式给出，**仅依赖加权比率 R⁻¹Q**，而非 Q、R 各自取值。

### 2.4 隐私目标：加权比率而非梯度
- 私有数据库定义为 D = {Rᵢ⁻¹Qᵢ}（所有智能体的加权比率）。
- **邻接关系**：两个数据库在至多一个智能体的加权比率相差不超过阈值 θ₁ 时视为相邻（有界邻接定义，Definition 1）。
- **集合论敏感性分析**（Theorem 3）：证明在相同的隐私预算 (ε,δ) 下，保护梯度所需噪声方差 ≥ 保护加权比率所需噪声方差。其核心逻辑是：梯度是 (Sᵢ, Rᵢ) 对在多对一映射下的像，删除一个梯度相当于删除整个等价类，而删除单个比率只删除一个元素，因而梯度邻接引发的输出扰动更大。

### 2.5 DP保证（Theorem 2）
- **窃听者假设**：外部窃听者位于发射机处（噪声方差最小，即最恶劣情形）。
- 针对状态依赖的敏感度问题，利用状态二阶矩有界性（此前收敛性分析保证）并借助马尔可夫不等式，给出敏感度超出给定阈值 C_Δ 的尾概率 δ″，并将其并入隐私松弛项。
- 逐步 DP 参数为：
  - εₜ ≤ (N_E·C_Δ)/(2s)·c(t) + (N_E·√C_Δ/√s)·√c(t)·Q⁻¹(δ′ₜ)
  - δₜ ≤ N_E·δ′ₜ + 2N_E²·Δ_K²(ρ′ξ+ρ′η)/C_Δ·c(t)
- 通过自适应序列组合定理（adaptive sequential composition）累积得到有限时域总隐私：
  - ε ≤ (N_E·C_Δ)/(2s)·Σc(t) + (N_E·√C_Δ/√s)·Σ√c(t)·Q⁻¹(δ′ₜ)
  - δ ≤ N_E·Σδ′ₜ + (2Δ_K²·N_E²(ρ′ξ+ρ′η))/C_Δ·Σc(t)
- 若 c(t) ∈ ℓ₁、δ′ₜ ∈ ℓ₁、√c(t)Q⁻¹(δ′ₜ) ∈ ℓ₁，则 **无限时域上 ε 有界、δ<1**，即实现无限时域有界累积DP而不依赖人工噪声。

### 2.6 收敛性分析
- **α = 2 情形（自由空间）**（Theorem 1）：若 c(t) ∈ ℓ₁ 且 c(t)→0，跟踪误差 ξ(t) 几乎必然（a.s.）且均方收敛到有限随机极限 ξ\*，且 E[ξ\*] 和 E‖ξ\*‖² 均有明确上界。
- **1 < α < 2 情形**（Proposition 1）：考虑对数正态遮蔽效应后，在 c(t) ∈ ℓ₁ 条件下同样实现 a.s. 与均方收敛，并给出 E‖ξ\*‖² 的上界表达式。
- **α > 2 情形**：出现超线性噪声放大，全局收敛性丧失，仅可能实现半全局收敛，文中归为未来工作。
- **不可同时满足的权衡（Corollary 1 vs. Theorem 2）**：若选择 c(t) ∈ ℓ₂ \ ℓ₁（如 c(t) = c/(t+1)）可实现追踪误差均方收敛到零（无偏收敛），但 ε 累积会发散；若选择 c(t) ∈ ℓ₁则 DP 有界但收敛到非零随机极限。**精确无偏收敛与无限时域有界DP不可兼得**。

---

## 3. 实验设计

- **仿真场景**：4个机器人组成链式拓扑（树图）在二维空间中运行，每个智能体为离散时间单积分器动力学。
- **通信噪声参数**：取自文献[47]实地测量结果——
  - 有障碍物场景：α₁ = 1.85，σ_dB = 3.2 dB，PL(d₀) = 59.7 dB；
  - 自由空间场景：α₂ = 2，σ_dB = 0；
  - 发射机噪声 sⱼ = 0.5，接收机噪声 zᵢ = 10⁻⁸。
- **成本权重**：对角矩阵 Qᵢ = diag(qᵢ, qᵢ)，Rᵢ = diag(rᵢ, rᵢ)，qᵢ ∈ Q = {2, 2.5, 3}，rᵢ ∈ R = {1, 1.5, 2}。
- **对比方法**：
  - 与参考文献[29]中保护"参考状态"的方案进行对比，展示保护目标不同导致的隐私效果差异；
  - 在理论上（Theorem 3）将加权比率与梯度两种隐私目标做敏感性对比。
- **评价指标**：累计隐私预算 ε(t)、累积失败概率 δ(t)、跟踪误差 ‖ξ(t)‖² 的均值和95%置信区间。

---

## 4. 资源与算力

- **论文未提及任何具体算力配置**（无 GPU 型号、数量、训练时长或计算平台信息）。
- 实验为低维数值仿真（4个智能体、二维状态、滚动时域 T = 10），计算复杂度很低，常规个人计算机即可完成，无需大规模算力资源。
- 为估计统计收敛特性，采用了蒙特卡洛（Monte Carlo）多次重复实验，但未明确给出重复次数。

---

## 5. 实验数量与充分性

- **实验组数**（三个子实验）：
  1. **收敛性与DP性能验证**：α = 2 与 α = 1.85 两种通信环境 × 两种惩罚因子 c₁(t) ∈ ℓ₁、c₂(t) ∈ ℓ₂\ℓ₁，共计约4组收敛场景；对DP参数 C_Δ ∈ {50, 100, 200, 400, 600} 做5组扫描；
  2. **隐私目标对比**（加权比率 vs. 参考状态）：1组对比实验。
- **充分性评述**：
  - **优点**：覆盖了定理1、推论1、命题1、定理2的主要理论预测；同时验证了隐私-性能不可兼得的权衡关系和 C_Δ 对累积隐私预算的影响；在统计层面给出95%置信区间和样例路径，展示几乎必然收敛性。
  - **不足**：实验以二维、4智能体小规模系统为主，缺乏大规模MAS（如数十或上百智能体）的仿真；缺乏对更复杂拓扑（环、网格）的验证（当前仅树图）；未在真实硬件/无线实验平台上验证物理噪声模型的可靠性；α = 2 情形缺少 Corollary 1 中 c(t) ∈ ℓ₂\ℓ₁ 的实际收敛曲线对比图（图2已含，但文中对 c₂(t) 的 DP 发散仅给出了命题性说明）；Theorem 3 的"加权比率优于梯度"的结论仅停留在理论推导，没有配套仿真实验。

---

## 6. 论文的主要结论与发现

1. **固有通信噪声可作为有效的自然隐私机制**：在路径损耗指数 α ≤ 2 的通信环境下，无需注入任何人工噪声，仅依靠物理信道噪声即可为智能体的加权比率 R⁻¹Q 提供无限时域上有界累积 (ε,δ)-差分隐私保证。
2. **跟踪性能得到保障**：在 c(t) ∈ ℓ₁ 条件下，编队跟踪误差 a.s. 收敛且均方收敛到有界随机极限，期望有界；在 c(t) ∈ ℓ₂\ℓ₁ 条件下可实现均方精确收敛。
3. **隐私与性能存在本质性权衡**：无限时域上有界 DP 保证与精确无偏收敛（误差均方收敛到零）不能同时实现——前者需要 c(t) 快速衰减（ℓ₁），后者需要 c(t) 缓慢衰减（ℓ₂，非ℓ₁）。
4. **隐私保护存在目标依赖性**：固有通信噪声对不同的隐私目标提供非均匀的保护强度——对加权比率的累积隐私泄露显著大于对参考状态的泄露（图4），说明自然噪声的隐私效能与被保护信息在控制机制中的结构性耦合方式密切相关。
5. **加权比率比梯度更适合作为隐私目标**：在添加/删除邻接关系下，保护加权比率所需噪声方差小于保护梯度所需噪声方差，即梯度包含冗余信息，直接保护梯度会过度消耗隐私预算。

---

## 7. 优点

- **概念创新性强**：首次系统性地提出并回答了"固有通信噪声能在多大程度上为分布式协同控制提供隐私保证"这一根本问题，与无线联邦学习中"免费隐私"的思想形成跨领域呼应。
- **物理建模贴合实际**：通信噪声模型综合了发射机畸变、接收机噪声、路径损耗和对数正态遮蔽，且方差随智能体间距离变化，真实反映了移动MAS的信道特性。
- **隐私目标选取具有洞察力**：将保护目标从完整代价函数/梯度收缩为加权比率，与LQR问题固有的尺度不变性相契合，有效缩小了隐私保护范围、降低了敏感性。
- **理论框架严谨、层次分明**：从收敛性分析（定理1、推论1、命题1）到隐私分析（定理2）再到敏感性对比（定理3），形成完整的数学证明体系；巧妙利用马尔可夫不等式处理状态依赖敏感度不可有界的问题，将尾概率纳入 δ 中，克服了传统全局灵敏度在连续状态空间中发散的根本障碍。
- **清晰揭示基本权衡**：严密论证了"精确收敛"与"有界隐私"不可兼得的本质困难，为此类系统的设计提供了方向性指导。
- **数值实验支撑充分**：对主要理论结果逐一验证，并补充了与参考状态隐私保护的对比实验，增强了结论的说服力。

---

## 8. 不足与局限

- **适用范围受限（α ≤ 2）**：论文的理论分析仅覆盖 α ≤ 2 的通信环境。α > 2（高障碍密度环境，如城市街区、室内场景）时会出现噪声随距离超线性放大，全局收敛性丧失，目前仅有半全局收敛的希望，尚缺乏解决方案。
- **窃听者位置假设过于保守**：假设窃听者恰位于发射机处（噪声最小），这是最恶劣情形。论文虽将此作为保守性考量，但实际中窃听者位置未知，这种假设会显著限制 c(t) 和 δₜ 的可选范围，导致隐私保证可能过度悲观。
- **状态独立 c(t) 的限制**：全局共享的标量衰减序列 c(t) 无法自适应地补偿噪声随状态距离变化的特性，在状态较大时稳定化漂移不足以对抗噪声放大，限制了大误差工况下的表现。
- **实验规模小、验证维度有限**：仅在4智能体、二维空间中进行数值仿真，缺乏大规模、更复杂拓扑、高维状态以及真实无线信道测试；C_Δ 等关键参数的选择规则未给出系统性的优化方法。
- **应用层面的隐私模型较理想化**：假设窃听者无法访问任何辅助信息（如其他传感器的局部测量），且未考虑恶意节点主动干扰或共谋攻击等更复杂的威胁模型；同时，通信噪声本身可能被合法接收方利用来更精确地估计状态，隐私与系统效用之间的张力细节未讨论。
- **"梯度 vs 比率"的对比条件较理想化**：Theorem 3 的证明依赖具体的数值优化设置（梯度下降+热启动），对不同优化器、不同迭代深度的普适性尚待考察；同时，何时 ΔD₃ᵢ(t) 严格大于 ΔD₂ᵢ(t)（即如何精确刻画该不等式的严格性条件）未解决。
- **缺乏实际工程考量**：真实硬件中的信道估计误差、同步误差、量化等非线性因素未纳入模型，限制了理论结果向实际系统迁移的直接性。

---

（完）
